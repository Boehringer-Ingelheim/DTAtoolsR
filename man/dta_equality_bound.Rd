% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateRules.R
\name{dta_equality_bound}
\alias{dta_equality_bound}
\title{Bound of an Equality or Set Operator Against a Numeric Column}
\usage{
dta_equality_bound(x, value)
}
\arguments{
\item{x}{The column vector taken from the table.}

\item{value}{The bound supplied for \code{equals}, \code{not_equals}, \verb{in}, or
\code{not_in}.}
}
\value{
\code{value} as numeric when \code{x} is numeric and every element of
\code{value} parses as a number; \code{value} unchanged otherwise.
}
\description{
\code{equals}/\code{not_equals}/\verb{in}/\code{not_in} compare \code{value} against the raw
column, so R's implicit coercion decides the outcome whenever the column
is numeric: \code{1e6 == "1000000"} renders the left side via \code{as.character()}
first and is \code{FALSE}, while \code{1000000L == "1000000"} is \code{TRUE} -- the
verdict then depends on int-vs-double storage, which legitimately differs
between the streamed (per-batch) and eager (whole-table) narrowing
decisions. This coerces \code{value} to numeric first, but only against a
numeric column, and only when every element of \code{value} parses; a bound
that does not parse, or a non-numeric column, is returned unchanged so the
comparison stays textual exactly as before.
}
\keyword{internal}
