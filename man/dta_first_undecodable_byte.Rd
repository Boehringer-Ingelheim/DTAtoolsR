% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTAFileTabular-class.R
\name{dta_first_undecodable_byte}
\alias{dta_first_undecodable_byte}
\title{Where in a Block the Decoding First Fails}
\usage{
dta_first_undecodable_byte(bytes, encoding, offset)
}
\arguments{
\item{bytes}{The raw block that failed.}

\item{encoding}{The declared source encoding.}

\item{offset}{How many source bytes precede this block.}
}
\value{
The 1-based byte offset in the file at which the first undecodable
line begins, or the block's own first byte when no single line reproduces
the failure.
}
\description{
A block is converted in pieces of tens of thousands of bytes, so
\code{\link[base:iconv]{base::iconv()}} answers NA for a whole piece and says nothing about where in
it the trouble is. "Somewhere in a 60 GB file" is not actionable, so the
failed block alone is re-converted a
line at a time to find the first line that does not decode. It runs only
after a conversion has already failed, and it is the last thing that happens
before the abort.
}
\keyword{internal}
