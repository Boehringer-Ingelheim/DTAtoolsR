% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateRules.R
\name{dta_condition_in_scope}
\alias{dta_condition_in_scope}
\title{Rows a Conditional Rule's IF Clause Applies To}
\usage{
dta_condition_in_scope(conditions, df, if_rows, numeric_cache = NULL)
}
\arguments{
\item{conditions}{The rule's IF clause.}

\item{df}{A data.frame.}

\item{if_rows}{The mask returned by \code{evaluate_conditions()} for \code{conditions}.}

\item{numeric_cache}{A named list from \code{\link[=dta_build_numeric_cache]{dta_build_numeric_cache()}}, or
\code{NULL} to convert each column on demand.}
}
\value{
A logical vector with one element per row of \code{df}, never \code{NA}.
}
\description{
Resolves the three-valued IF mask into the two-valued "is this row in scope"
answer the rule needs.
}
\details{
\code{NA} in the IF clause has two causes that must be resolved differently:
\itemize{
\item the value is \strong{missing} -- nothing is known about the row, so the
condition genuinely does not apply and the row is out of scope.
\item the value is \strong{unconvertible} -- the row stays in scope. Dropping it
would let a row whose THEN clause definitively fails escape the rule
altogether, which is exactly what \code{\link[=dta_as_numeric_strict]{dta_as_numeric_strict()}} forbids: an
unconvertible value must not be treated as passing.
}

A row that fails some \emph{other} predicate of the clause outright is out of
scope regardless, because the clause is a conjunction and one determinate
\code{FALSE} settles it.
}
\keyword{internal}
