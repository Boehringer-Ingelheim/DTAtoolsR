% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualification.R
\name{qualification_requirements}
\alias{qualification_requirements}
\title{Qualification requirements shipped with the package}
\usage{
qualification_requirements(area = NULL, dir = NULL)
}
\arguments{
\item{area}{Optional character. Restrict the result to one functional area,
for example \code{"VAL"}. Case-insensitive. \code{NULL} returns every requirement.}

\item{dir}{Optional character. Directory holding the qualification suite.
Defaults to the copy installed with the package.}
}
\value{
A data frame with one row per requirement and the columns \code{id},
\code{area}, \code{area_title}, \code{text}, \code{risk}, \code{category}, \code{covers} (a list column
of exported symbol names), \code{source} and \code{file}.
}
\description{
Reads the requirement specifications that the qualification suite verifies.
Each requirement states one testable property of the software, carries a
risk rating and a category, and names the exported symbols it constrains.
The suite's test titles reference these identifiers, which is how the
traceability matrix in a qualification report is derived rather than
maintained by hand.
}
\examples{
reqs <- qualification_requirements()
nrow(reqs)
head(reqs[, c("id", "risk", "category")])
}
\seealso{
\code{\link[=run_qualification]{run_qualification()}} executes the suite that verifies them.
}
