% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateBlocks.R
\name{.tv_mark_block_paragraphs}
\alias{.tv_mark_block_paragraphs}
\title{Stamp every template paragraph that is wholly a block placeholder}
\usage{
.tv_mark_block_paragraphs(xml_path, nonce, variables = list())
}
\arguments{
\item{xml_path}{Character. Path to the template's \code{word/document.xml}.}

\item{nonce}{Character. This export's nonce, from \code{\link[=.tv_block_nonce]{.tv_block_nonce()}}.}

\item{variables}{Named list of placeholder values; keys here take precedence.}
}
\value{
A named character vector mapping sentinel to the token it replaced,
in document order. Empty when the template has no block placeholder.
}
\description{
Called on the template's \code{word/document.xml} \emph{before} any substitution, and
the reason the two passes can be told apart at all. Identifying block
paragraphs by their text in the finished document instead would conflate two
different things -- what the template asked for, and what a substituted value
happens to spell -- so a DTA whose title is the literal string
\code{"{COLUMN_SPECS}"} would have its title paragraph silently replaced by a
table. The nonce is drawn per export, so no value can predict or contain it.
}
\details{
Only \emph{top-level} body paragraphs are stamped, which is also what keeps a
placeholder inside a table from taking the table with it:
\code{officer::cursor_reach()} matches the text of a top-level body child, and
\code{xml2::xml_text()} of a \code{w:tbl} concatenates every cell, so a one-cell table
whose only content is the token would match as a whole. Unstamped, it is left
alone and reported instead.

A token the caller supplied a value for is left unstamped too: \code{variables}
wins over a block, and pass 1 needs the token intact to substitute into.
}
\keyword{internal}
