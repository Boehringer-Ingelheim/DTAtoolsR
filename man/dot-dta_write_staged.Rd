% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportDocuments.R
\name{.dta_write_staged}
\alias{.dta_write_staged}
\title{Write a document to a staging file, then move it into place}
\usage{
.dta_write_staged(path, write, check = NULL)
}
\arguments{
\item{path}{Character. Final destination.}

\item{write}{Function of one argument (the staging path) that produces the
file there. Its return value is ignored.}

\item{check}{Optional function of one argument (the staging path) that
signals an error (typically via \code{cli::cli_abort()}) if the staged file is
unacceptable. Runs after \code{write()} and before the file is moved into
place.}
}
\value{
Invisibly returns \code{path}.
}
\description{
Every document writer in this file, and the template writer in
\code{exportTemplateDocx.R}, produces its output through this helper instead of
writing straight to the destination. \code{officer::print(target = )},
\code{writeLines()} and the PDF backends all truncate or create their target
before the write finishes, so a failure partway used to leave a corrupt (or
entirely wiped) file where a good export -- possibly one nobody has touched
in months -- used to be. That is worse than aborting with nothing written:
the destination must come out of a failed export exactly as it went in.
}
\details{
Mirrors the stage-then-rename pattern already used by
\code{write_table_to_file()} (\code{R/DTADataSetTabular-class.R}): the staging file is
created in \code{dirname(path)} so the final step is a rename within one
filesystem rather than a copy across two, and it is named with the same
extension as \code{path} because officer and pandoc both dispatch on the
extension of their output path.
}
\keyword{internal}
