% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateBlocks.R
\name{.tv_stamp_paragraph}
\alias{.tv_stamp_paragraph}
\title{Stamp one paragraph's text runs with a sentinel, blanking the rest}
\usage{
.tv_stamp_paragraph(p, sentinel)
}
\arguments{
\item{p}{An \code{xml_node} for the paragraph to stamp.}

\item{sentinel}{Character. The sentinel text to write into the first run.}
}
\value{
\code{TRUE} if a run was found and stamped, \code{FALSE} otherwise.
}
\description{
Shared by \code{\link[=.tv_mark_block_paragraphs]{.tv_mark_block_paragraphs()}} (stamping the template's own block
paragraphs) and \code{\link[=.tv_bind_region_block]{.tv_bind_region_block()}} (stamping a bare dataset block
inside a freshly-copied region repetition). A paragraph with no text run at
all -- possible for an empty or purely-graphical paragraph -- cannot carry
a sentinel, so it is left alone and reported as \code{FALSE}.
}
\keyword{internal}
