% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateDocx.R
\name{dta_template_placeholders}
\alias{dta_template_placeholders}
\title{List the placeholders a Word template can use}
\usage{
dta_template_placeholders(dta = NULL)
}
\arguments{
\item{dta}{Optional \link{DTA} object. When supplied, the returned values are the
resolved text for that object's inline placeholders; a block placeholder
has no text value of its own, so its entry stays its description. When
\code{NULL} (the default) every entry is a short description of what the
placeholder means.}
}
\value{
A named character vector whose names are the brace-delimited
placeholder tokens (\code{"{DTA_TITLE}"} and so on), in documentation order.
The values are \describe{
\item{descriptions}{when \code{dta} is \code{NULL}.}
\item{the resolved text}{when \code{dta} is a \link{DTA}, for inline placeholders;
a block placeholder keeps its description either way.}
}
The returned vector also carries a \code{kind} attribute: a character vector,
named like the return value, of \code{"inline"} or \code{"block"}.
}
\description{
\code{\link[=export_with_template]{export_with_template()}} fills a fixed set of \code{{PLACEHOLDER}} markers from a
\link{DTA} object. Call this to discover which markers exist -- and, given a
\code{DTA}, what each one would currently expand to -- rather than exporting a
document to find out, or reading the list out of the documentation by hand.
}
\details{
A placeholder is either \strong{inline} (resolves to a string substituted into a
run of text, and may sit mid-sentence) or a \strong{block} (replaces a whole
paragraph with rendered Word content -- a table, a heading, a list -- so it
must be the only text in its paragraph, in the document body). The two kinds
share one token grammar and are told apart by the \code{kind} attribute of the
returned vector, not by the vector's type.
}
\examples{
# What can a template refer to?
head(dta_template_placeholders())

# A block placeholder's description, and how to tell it from an inline one.
dta_template_placeholders()[["{COLUMN_SPECS}"]]
attr(dta_template_placeholders(), "kind")[["{COLUMN_SPECS}"]]

# What would those become for a real DTA?
dta <- read_dta_from_yaml(
  system.file("extdata", "clinical_dta.yaml", package = "DTAtools")
)
dta_template_placeholders(dta)[c("{DTA_TITLE}", "{SUPPLIER_NAME}")]
}
\seealso{
\code{\link[=export_with_template]{export_with_template()}}, \code{\link[=write_dta]{write_dta()}}
}
