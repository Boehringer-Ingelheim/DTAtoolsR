% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTADataSetTabular-class.R,
%   R/DTADataSetFile-class.R
\name{clear_validation}
\alias{clear_validation}
\alias{clear_validation,DTAtools::DTADataSetFile-method}
\title{Clear Validation State}
\usage{
clear_validation(x, ...)

clear_validation(x, ...)
}
\arguments{
\item{x}{A \code{DTADataSetTabular} or \code{DTADataSetFile} object --
this page documents both \code{clear_validation()} methods.}

\item{...}{Additional arguments:
\describe{
\item{tables}{NULL (default), character target names, or numeric target
indices.}
\item{remove_artifacts}{Logical. If TRUE, delete artifact files for the
selected targets.}
}}
}
\value{
Invisibly returns \'x\'.

Invisibly returns \code{x}.
}
\description{
Clears in-memory validation state for one or all tables.

Clears in-memory validation state for one or all file targets.
}
\examples{
path <- tempfile(fileext = ".txt")
writeLines("content", path)
ds <- check(DTADataSetFile(name = "delivery", paths = path), quiet = TRUE)
nrow(validation_status(clear_validation(ds)))
}
