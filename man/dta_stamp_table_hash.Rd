% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importConversion.R
\name{dta_stamp_table_hash}
\alias{dta_stamp_table_hash}
\title{Stamp an Arrow Table With a Digest of Its Contents}
\usage{
dta_stamp_table_hash(table, hash)
}
\arguments{
\item{table}{An Arrow table, or any other object (returned unchanged).}

\item{hash}{A length-1 character digest.}
}
\value{
The stamped table, or \code{table} unchanged when it is not an Arrow table
or the stamp could not be applied.
}
\description{
Records \code{hash} on the table under \link{dta_table_hash_key} and returns it.

An arrow table is an R6 object -- an environment -- so this attaches the
stamp to the object itself rather than copying it: every reference to that
table sees the stamp, and only that table carries it. A table Arrow builds
anew from it (\code{concat_tables()}, \code{Slice()}, \code{[}, a computed \code{dplyr} query)
is a different object and carries no stamp, which is exactly the required
behaviour -- its contents are not the contents that were hashed.
}
\keyword{internal}
