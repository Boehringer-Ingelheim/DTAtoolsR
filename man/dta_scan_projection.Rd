% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingScale.R
\name{dta_scan_projection}
\alias{dta_scan_projection}
\title{Columns a Streaming Scan Actually Needs}
\usage{
dta_scan_projection(specs, rules_list, schema_names)
}
\arguments{
\item{specs}{A \code{DTAColumnSpecCollection}.}

\item{rules_list}{A list of \code{DTARule} objects, or \code{NULL}.}

\item{schema_names}{Character. The full set of column names in the
source's Arrow schema.}
}
\value{
A character vector of column names, ordered as in \code{schema_names}
(so batch column order stays stable), or \code{NULL} when projecting would
not help: some rule's columns cannot be enumerated (see
\code{\link[=dta_rule_all_columns]{dta_rule_all_columns()}}) and might read anything, or the needed columns
turn out to be empty or already everything.
}
\description{
The projection to hand to a Dataset \code{Scanner}: the columns the column specs
declare, plus every column any rule reads, and nothing else.

Projection means a column outside that set is never parsed, never
converted, and never materialised into an R vector at all -- it is the
difference between a batch carrying 30 columns and one carrying all 200 a
wide file might have, for work (column spec checks, rule evaluation, import
typing) that was only ever going to look at 30 of them.
}
\keyword{internal}
