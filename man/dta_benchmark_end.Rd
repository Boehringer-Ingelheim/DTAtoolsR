% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking.R
\name{dta_benchmark_end}
\alias{dta_benchmark_end}
\title{End a Benchmark Measurement}
\usage{
dta_benchmark_end(state, rows = NA_real_)
}
\arguments{
\item{state}{The state list returned by \code{\link[=dta_benchmark_begin]{dta_benchmark_begin()}}, or \code{NULL}.}

\item{rows}{Numeric. Row count to attach to the metrics (for
\code{rows_per_sec}), or \code{NA_real_} when no trustworthy count is available at
the call site. Anything that is not a single number -- including the
\code{NULL} a caller gets from reading an absent attribute -- is recorded as
\code{NA_real_}.}
}
\value{
A one-row \code{data.frame} of metrics, or \code{NULL} when \code{state} is
\code{NULL}.
}
\description{
Closes a measurement window opened by \code{\link[=dta_benchmark_begin]{dta_benchmark_begin()}} and returns
one row of metrics, or \code{NULL} when \code{state} is \code{NULL} (benchmarking was off,
or the nesting guard declined this call).

\code{proc.time()} is read \emph{first}, before anything else, so that none of the
bookkeeping below -- including the closing \code{gc()} -- is charged to the
measured region. The active flag is reset via \code{on.exit(add = TRUE)} so a
measurement that errors still leaves the nesting guard usable for the next
call.
}
\keyword{internal}
