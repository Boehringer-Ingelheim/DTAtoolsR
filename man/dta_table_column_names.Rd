% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingValidation.R
\name{dta_table_column_names}
\alias{dta_table_column_names}
\title{Column Names of a Table, However It Is Held}
\usage{
dta_table_column_names(table)
}
\arguments{
\item{table}{A table representation.}
}
\value{
Character. The column names, or \code{character(0)} when they cannot be
determined cheaply.
}
\description{
The column names of a table representation, read without scanning it.

The structural gate decides a missing required column from the header alone,
so it has to know the columns BEFORE any row is read -- and by then the
table may be an Arrow \code{Table}, a \code{Dataset}, an \code{arrow_dplyr_query}, a
\code{RecordBatchReader} or a plain data frame. \code{names()} answers for all of
them, and on a reader it reports the schema without pulling a batch, so a
consumable source is not spent merely by being inspected. \code{names(x$schema)}
is not a substitute: an \code{arrow_dplyr_query} has no \verb{$schema}.

Anything else yields \code{character(0)}, which callers read as "not knowable
cheaply" and answer by scanning exactly as they always did. That is
deliberately not an error: an unfamiliar holding is a reason to fall back,
not a reason to fail.
}
\keyword{internal}
