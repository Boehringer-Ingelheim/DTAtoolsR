% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importConversion.R
\name{dta_arrow_numeric_state}
\alias{dta_arrow_numeric_state}
\title{Per-Scan State for the Arrow Numeric Fast Path}
\usage{
dta_arrow_numeric_state(type_map)
}
\arguments{
\item{type_map}{Named character vector from \code{\link[=dta_compile_spec_types]{dta_compile_spec_types()}}.}
}
\value{
An environment, or \code{NULL}.
}
\description{
Everything \code{\link[=dta_arrow_parse_numeric_batch]{dta_arrow_parse_numeric_batch()}} needs that does not change from
batch to batch: the compiled target types, the two Arrow \code{DataType} objects
the cast uses, the replacement \code{Field} of each column once it has been seen,
and the per-column backoff counters. Built once per scan because
constructing an Arrow object is an R6 construction and a hop into C++, and
this path runs once per declared-numeric column per batch, for every batch
of a file of any size.

Returns \code{NULL} -- which the batch function treats as "do nothing" -- when the
diagnostic switch \code{DTAtools.stream_arrow_numeric} is off, or when the specs
declare no numeric column at all, so that the whole fast path costs a single
\code{is.null()} per batch in the cases where it can do nothing.
}
\keyword{internal}
