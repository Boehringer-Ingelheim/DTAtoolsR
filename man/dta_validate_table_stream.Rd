% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingValidation.R
\name{dta_validate_table_stream}
\alias{dta_validate_table_stream}
\title{Validate a Table from a Stream of Record Batches}
\usage{
dta_validate_table_stream(
  specs,
  reader,
  verbose = FALSE,
  max_errors = getOption("DTAtools.max_errors", 10000L),
  coerce = TRUE,
  fail_fast = FALSE,
  precomputed = list(),
  known_columns = character(0)
)
}
\arguments{
\item{specs}{A \code{DTAColumnSpecCollection}.}

\item{reader}{An object with a \code{read_next_batch()} method.}

\item{verbose}{Logical. Print progress. While scanning, a line reporting the
rows read so far and the current rate is emitted at most once every
\code{getOption("DTAtools.progress_seconds", 30)} seconds, so a scan that runs
for hours is distinguishable from a hang. The interval is measured from the
start of the scan, so a run shorter than it prints nothing extra. There is
no percentage and no ETA: a stream has no total row count to compute either
from.}

\item{max_errors}{Integer, or \code{NULL} to retain everything. Cap on retained
per-cell errors, defaulting to \code{getOption("DTAtools.max_errors", 10000L)}
so that the option is honoured wherever a sink is created, not only at the
outer entry points. \code{NULL} retains everything, matching the materialising
path.}

\item{coerce}{Logical. Type each batch against the specs as it arrives,
recording values that cannot be represented. This is the streaming
equivalent of typing the table once at import.}

\item{fail_fast}{Logical. Stop at the first batch that shows any problem
instead of scanning to the end. The result then carries a \code{partial_scan}
attribute and axes that could not be settled report \code{NA}.}

\item{precomputed}{A list parallel to \code{specs@rules}: entry \code{i} is a
finalise-shaped result (\code{id}/\code{valid}/\code{message}) for a rule already
answered outside the batch loop -- today, a uniqueness rule computed by
Arrow's grouped aggregation over the whole dataset (see
\code{\link[=dta_stream_unique_precompute]{dta_stream_unique_precompute()}}) -- or \code{NULL} for a rule this scan must
still accumulate. Precomputed rules are skipped per batch and their
result is spliced in at finalise, in rule order.}

\item{known_columns}{Character. The source's full column names, when the
caller can read them without consuming the source. Used only when the
stream yields no rows: rules are then evaluated once against an empty
table with these columns, so a rule naming a column the table lacks
still reports "could not be evaluated" exactly as the materialising path
does -- previously a header-only file certified such rules as passed.}
}
\value{
A \code{details} list of the same shape \code{validate_table_detailed()}
returns, plus \code{import_typing_errors}: the import-typing frame on its own,
before it is merged with the rule-time import errors into \code{import_errors}.
That is the axis a materialising load records in a dataset's
\code{import_issues} property, so a caller wanting to record the same thing for
a streamed table reads this rather than the merged frame. Always present --
a zero-row frame carrying \code{n_import_errors = 0} when no value failed to
type -- so that a consumer can tell an empty axis from a result written
before the field existed, which is the difference between recording nothing
and falling back to the merged frame. Consumers that do not know the field
ignore it: it is an extra list element, and nothing enumerates \code{details}.
}
\description{
The streaming counterpart of \code{validate_table_detailed()}. Evaluates all three
axes -- column specs, rules, and import typing -- reading one batch at a
time, and returns the same \code{details} structure, so every existing consumer
(\code{results()}, \code{messages()}, \code{inspect()}, the Shiny app) works unchanged.

Nothing here scales with the number of rows. Peak memory is bounded by the
batch size for the column spec axis, by the number of distinct keys for uniqueness
rules, by the number of distinct groups for grouped rules, and by the
retained-error cap.

Row numbers are positions in the input, not in the batch a value happened to
fall in.
}
\keyword{internal}
