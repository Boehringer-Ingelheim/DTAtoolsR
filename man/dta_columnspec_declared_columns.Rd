% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columnSpecChecks.R
\name{dta_columnspec_declared_columns}
\alias{dta_columnspec_declared_columns}
\title{Which Columns Declare Each Constraint}
\usage{
dta_columnspec_declared_columns(schemas)
}
\arguments{
\item{schemas}{Compiled schemas, from \code{dta_compile_columnspec_schemas()}.}
}
\value{
A named list, one character vector of column names per keyword.
}
\description{
Reads the compiled schemas to find, for each keyword, the columns whose spec
declares it. This is the denominator of the per-check report: a keyword no
column declares is \code{not_applicable}, never \code{passed}, because reporting a pass
for a check that could not run is the same hollow certificate \code{check()}
refuses to issue for a spec that declares no columns at all.

Declaration, not evaluation: \code{maxLength} and \code{pattern} are evaluated only
against string columns (see \code{dta_check_column_spec()}), so a column that
declares a length but holds numbers is counted here and skipped there. Such a
column always fails the \code{type} check as well, so the mismatch is still
reported -- on the axis that can name it.
}
\keyword{internal}
