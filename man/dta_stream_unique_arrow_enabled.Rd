% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingScale.R
\name{dta_stream_unique_arrow_enabled}
\alias{dta_stream_unique_arrow_enabled}
\title{Should Arrow Answer Uniqueness Rules Ahead of the Streaming Scan?}
\usage{
dta_stream_unique_arrow_enabled()
}
\value{
A single, non-\code{NA} logical.
}
\description{
Unlike \code{\link[=dta_use_arrow_compute]{dta_use_arrow_compute()}} (\code{options(DTAtools.use_arrow_compute = )},
strictly opt-in because a future arrow release could change floating-point
kernel semantics -- rounding, NaN handling -- and silently change a
validation verdict for a user who never asked for it), this path is on by
default.

It can be, because it is restricted to \code{utf8} (text) key columns (see
\code{\link[=dta_arrow_unique_eligible]{dta_arrow_unique_eligible()}}): Arrow's grouped aggregation and R's
\code{duplicated()} agree byte-for-byte on strings, with none of the
floating-point latitude that keeps the general Arrow-compute path opt-in.
What tips the default the rest of the way is that the R fallback -- a
\code{fastmap} accumulator holding one entry per distinct key -- is exactly the
component that runs out of memory at the scale streaming exists for.

\code{options(DTAtools.stream_arrow_unique = FALSE)} restores the per-batch
accumulator unconditionally, for a user who wants to rule out any
behavioural difference at all.
}
\keyword{internal}
