% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTAMetaData-class.R
\name{.parse_metadata_date}
\alias{.parse_metadata_date}
\title{Parse a Metadata Date Field}
\usage{
.parse_metadata_date(value, field, require_date = FALSE)
}
\arguments{
\item{value}{The supplied value.}

\item{field}{Character. Field path used in the import issue record.}

\item{require_date}{Logical. When \code{TRUE} the field is typed \code{Date}
and a phrase cannot be stored, so an unparseable string is coerced the way
\code{as.Date()} would coerce it (to \code{NA}) - and, unlike the
\code{FALSE} case where a phrase is legitimate free text, that \code{NA}
is recorded as an import issue, since a Date-typed field silently losing
its value is exactly the discard \code{@import_issues} exists to catch.}
}
\value{
A list with \code{value} (the value to store) and \code{issues} (a
possibly empty list of import issue records).
}
\description{
Converts a metadata date string to a \code{Date} without silently discarding
anything. A bare ISO date converts cleanly; a date followed by qualifying text
converts but reports an import issue carrying the original string; a string
with no leading ISO date is a free-text phrase and is returned untouched.
}
\keyword{internal}
