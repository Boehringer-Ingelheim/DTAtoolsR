% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTARuleColCondition-class.R
\name{DTARuleColCondition}
\alias{DTARuleColCondition}
\title{DTARuleColCondition Class}
\usage{
DTARuleColCondition(id, description = NULL, condition = NULL, then = NULL)
}
\arguments{
\item{id}{Character. A unique identifier for the rule.}

\item{description}{Character or NULL. Free-text description of the rule.}

\item{condition}{List. Conditions to check, as a mapping of column name to
one or more operators, e.g. \code{list(AGE = list(greater = 18, less = 65))}.
All operators given for a column must hold. A YAML sequence of
single-column mappings is accepted and normalised to this form.}

\item{then}{List. Conditions that must be true if the \code{condition} clause is
met. Same structure as \code{condition}.}
}
\value{
An object of class \code{DTARuleColCondition}.
}
\description{
A rule for validating data tables with conditions
}
\examples{
# Create a check_range rule
DTAtools::DTARuleColCondition(
  id = "check_gfreasnd2",
  condition = list(
    GFREASND = list(
      empty = FALSE
    )
  ),
  then = list(
    GFSTAT = list(
      empty = FALSE
    ),
    GFORRES = list(
      empty = TRUE
    )
  )
)
}
