% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reportRendering.R
\name{.report_condition_to_text}
\alias{.report_condition_to_text}
\title{Human-Readable Text for a Condition Map}
\usage{
.report_condition_to_text(cond)
}
\arguments{
\item{cond}{A named list, or \code{NULL}/empty/not list-shaped.}
}
\value{
A length-1 character string (possibly \code{""}), HTML-escaped.
}
\description{
Converts a condition map -- a named list keyed by column, each value a
list of one or more \code{operator = value} pairs -- into one readable phrase,
e.g. \code{"AGE &gt;= 18 and STATUS = COMPLETED"}. A column can legitimately
carry more than one operator at once (e.g. \verb{WEIGHT: \{min: 1, max: 2\}};
\code{evaluate_condition()} in \code{R/evaluateRules.R} ANDs every operator
supplied), so every operator for a column is rendered, not just the
first. Used for \code{DTARuleColCondition}'s \code{condition}/\code{then} and each named
entry of \code{DTARuleGroupCondition@conditions}.
}
\keyword{internal}
