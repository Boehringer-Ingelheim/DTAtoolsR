% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateRules.R
\name{rule_check_group_condition}
\alias{rule_check_group_condition}
\title{Rule: check_group_condition}
\usage{
rule_check_group_condition(rule, df, numeric_cache = NULL)
}
\arguments{
\item{rule}{A \code{DTARuleGroupCondition} object.}

\item{df}{A data.frame to validate.}

\item{numeric_cache}{A named list from \code{\link[=dta_build_numeric_cache]{dta_build_numeric_cache()}}, or
\code{NULL} to convert each column on demand.}
}
\value{
A list with elements \code{id}, \code{valid}, and \code{message}.
}
\description{
Evaluates named conditions per group and applies constraints between those
condition outcomes.
}
\examples{
df <- data.frame(
  SUBJECT_ID = c("S1", "S1", "S2"),
  STATUS = c("FAILED", "FAILED", "DONE"),
  RESULT = c(NA, 12, NA),
  stringsAsFactors = FALSE
)
rule <- DTARuleGroupCondition(
  id = "group_example",
  group_by = "SUBJECT_ID",
  conditions = list(
    c_failed = list(STATUS = list(equals = "FAILED")),
    c_reported = list(RESULT = list(empty = FALSE))
  ),
  constraints = list(
    list(type = "mutually_exclusive", left = "c_failed", right = "c_reported")
  )
)
rule_check_group_condition(rule, df)
}
