% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateRules.R
\name{dta_as_numeric_strict}
\alias{dta_as_numeric_strict}
\title{Strict Numeric Conversion}
\usage{
dta_as_numeric_strict(x)
}
\arguments{
\item{x}{A vector taken from a table column.}
}
\value{
A list with \code{values} (numeric), \code{raw} (character, the source text
verbatim), \code{missing} (logical) and \code{unconvertible} (logical), each the same
length as \code{x}.
}
\description{
Converts a column to numeric while keeping apart the three cases that a bare
\code{as.numeric()} collapses into a single \code{NA}:
\itemize{
\item \strong{missing} -- \code{NA} or an empty string in the \emph{source}. Not an error: a
missing value neither passes nor violates a numeric rule.
\item \strong{unconvertible} -- present in the source but not representable as a
number (\code{"ninety"}, \code{"N/A"}, \code{">65"}, the factor level \code{"high"}). This is
an import error, and the row must not be treated as passing the rule.
\item \strong{convertible} -- the numeric value is used.
}

Factors are converted through \code{as.character()} \strong{first}. \code{as.numeric()} on a
factor returns its \emph{integer level codes}, so \code{factor(c("500", "600", "700"))}
reads as \verb{1, 2, 3} and sails through any range rule that admits small
integers.

Only unrecoverable values are reported as unconvertible. A value that
converts but changes representation (\code{"007"} to \code{7}, \code{"1.50"} to \code{1.5}) is a
clean conversion, not an import error.

Dates and date-times are converted through their own numeric representation
and can never be unconvertible; they are never text that failed to parse.
}
\keyword{internal}
