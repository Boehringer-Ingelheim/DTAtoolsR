% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingValidation.R
\name{dta_unexpected_column_errors}
\alias{dta_unexpected_column_errors}
\title{Undeclared Columns as Column Spec Errors}
\usage{
dta_unexpected_column_errors(findings)
}
\arguments{
\item{findings}{A list from \code{dta_structure_findings()}.}
}
\value{
A data frame of errors, or \code{NULL} when every column is declared.
}
\description{
Renders the unexpected half of a structural finding in the shape the per-row
column spec axis uses, so an undeclared column is reported as an ordinary
column spec violation -- carried in \code{full_error}, counted in
\code{n_columnspec_errors}, and surfaced by \code{messages()} -- rather than as a
console warning nothing downstream can act on.

The mirror image of \code{\link[=dta_missing_column_errors]{dta_missing_column_errors()}}: \code{column} names the column,
because it is really there in the table, and \code{columnspec} is \code{NA}, because no
spec describes it. That is the whole finding.

One row per undeclared column, with \code{row = NA}. Not one per table row: the
finding is a fact about the table's shape, decided from its names, and it is
no more true of row 400,000,000 than of row 1.
}
\keyword{internal}
