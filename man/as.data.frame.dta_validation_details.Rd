% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validationFunctions.R
\name{as.data.frame.dta_validation_details}
\alias{as.data.frame.dta_validation_details}
\title{Coerce Validation Details to a Data Frame}
\usage{
\method{as.data.frame}{dta_validation_details}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{dta_validation_details} object, as returned by
\code{validation_errors()}.}

\item{row.names}{\code{NULL} or a character vector of row names.}

\item{optional}{Logical, passed to the default method's contract; unused.}

\item{...}{Ignored.}
}
\value{
A data.frame with one row per column spec error, followed by one row per
rule failure, followed by one row per import error, and columns \code{source},
\code{rule_id}, \code{row}, \code{column}, \code{keyword} and \code{message}.
}
\description{
Flattens the detailed validation output for one table into one row per
reported error.

The raw list cannot be coerced by the default method: \code{columnspec_errors} bundles
a \emph{grouped} summary table with the \emph{ungrouped} full error table, and those two
have different row counts, so \code{as.data.frame()} failed with "arguments imply
differing number of rows". This method flattens the errors themselves instead.
}
\examples{
ds <- check(
  create_example_DTADataSetTabular(2),
  tables = "tab1",
  persist = FALSE,
  quiet = TRUE
)
errors <- as.data.frame(validation_errors(ds, table = "tab1"))
head(errors)
}
