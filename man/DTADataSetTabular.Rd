% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTADataSetTabular-class.R
\name{DTADataSetTabular}
\alias{DTADataSetTabular}
\title{DTADataSetTabular Class}
\usage{
DTADataSetTabular(
  name,
  specs,
  tables = list(),
  files = list(),
  description = NULL,
  template_source = NULL,
  template_version = NULL,
  template_date = NULL
)
}
\arguments{
\item{name}{Character. Name of the container.}

\item{specs}{A DTAColumnSpecCollection object specifying the column specs.}

\item{tables}{A named list of tabular objects. A materialised entry
(data.frame or Arrow Table) is typed by \code{specs} and converted to an Arrow
Table before storage. A lazy entry (Arrow Dataset, \code{arrow_dplyr_query}, or
RecordBatchReader) is stored exactly as given -- it is typed and checked
for import issues later, at scan time, in \code{check()}.}

\item{files}{A list of DTAFile objects specifying input file information.}

\item{description}{Character or NA. Free-text description of the dataset.}

\item{template_source}{Character or NA. Source of the template used to
generate the dataset specification.}

\item{template_version}{Character or NA. Version of the template used.}

\item{template_date}{Character or NA. Date of the template used.}
}
\value{
An object of class DTADataSetTabular
}
\description{
Handles tabular data with column specifications and rules.
}
\details{
A table handed to this constructor is a table already in memory, which is
the case \code{\link[=check]{check()}}'s retained-error cap is least suited to. \code{max_errors}
(default \code{getOption("DTAtools.max_errors", 10000L)}) exists because on a
large dirty file retention is one row per bad cell and an unbounded error
frame exhausts memory; a table small enough to have been built in R has
nothing to protect. Check such a table with \code{check(..., max_errors = Inf)}
when the complete per-cell detail is wanted -- there is no spill on the
materialising path, so detail dropped by the cap is recovered only by
checking again. The cap never affects the answer: the reported counts and
the verdict on every axis are exact at any cap, including the default.
}
\examples{
# Create sample tables
table1 <- data.frame(STUDYID = c("1234", "1234", "1234"), VISIT = c("V03", "V03", "EOT"))
table2 <- data.frame(STUDYID = c("1234", "1234", "1234"), VISIT = c("EOT", "V05", "EOT"))

# List of tables
tables <- list(table1 = table1, table2 = table2)

# Create the DTADataSetTabular object
data_obj <- DTADataSetTabular(
  name = "example",
  specs = create_example_DTAColumnSpecCollection(1),
  tables = tables
)
}
