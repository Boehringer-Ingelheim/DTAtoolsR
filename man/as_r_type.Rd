% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTAColumnSpecStructureSAS-class.R
\name{as_r_type}
\alias{as_r_type}
\title{as_r_type}
\arguments{
\item{x}{A \code{DTAColumnSpecStructure} object.}
}
\value{
A length-1 character naming an R type: one of \code{"character"},
\code{"double"}, \code{"integer"} or \code{"logical"}. Defaults to \code{"character"} when the
spec declares no type, so an unspecified column is never coerced.
}
\description{
The R storage type a declared column type maps to. This is the import-time
sibling of \code{\link[=as_json_schema_type]{as_json_schema_type()}}: that generic says how a column is
\emph{validated}, this one says how it is \emph{stored} when the table is read.

The two must agree, so every declared type whose JSON Schema type is
\code{"string"} maps to \code{"character"} here. \code{Date}, \code{Time} and \code{DateTime} are
validated as strings by their pattern and format, so they are stored as the
text that was read rather than parsed into \code{Date}/\code{POSIXct} -- parsing them
would re-render the value and validate something other than what the file
contained.

\code{Char} maps to \code{"character"}, which is what stops the import layer from ever
touching a character column: a \code{SUBJECT_ID} of \code{"007"} must survive import
unchanged, and any numeric round trip would silently make it \code{7}.
}
\examples{
as_r_type(DTAColumnSpecStructureSAS(type = "Num"))
as_r_type(DTAColumnSpecStructureSAS(type = "Char", format = "$12.", length = 12))
}
