% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateRules.R
\name{dta_condition_violated}
\alias{dta_condition_violated}
\title{Rows an IF/THEN Rule Counts as Violations}
\usage{
dta_condition_violated(rule, df)
}
\arguments{
\item{rule}{A conditional rule.}

\item{df}{A data.frame.}
}
\value{
A logical vector with one element per row, never \code{NA}.
}
\description{
The per-row violation mask for a conditional rule: the IF applies but the
THEN does not hold, where a missing or unconvertible THEN counts against the
row. Which rows the IF applies to is decided by \code{\link[=dta_condition_in_scope]{dta_condition_in_scope()}}.

This is the single definition of a conditional violation. \code{rule_check_col_condition()},
the streaming scan and \code{inspect()}'s row lookup all call it, so a batched
scan, a whole-table pass and the row preview cannot disagree.
}
\keyword{internal}
