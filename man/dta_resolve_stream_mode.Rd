% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_helpers.R
\name{dta_resolve_stream_mode}
\alias{dta_resolve_stream_mode}
\title{Decide Whether to Stream a File}
\usage{
dta_resolve_stream_mode(
  stream = getOption("DTAtools.stream", "auto"),
  file = NULL
)
}
\arguments{
\item{stream}{One of \code{"auto"}, \code{"always"}, \code{"never"}, or a single logical.
Defaults to \code{getOption("DTAtools.stream")}, itself defaulting to \code{"auto"}.}

\item{file}{Path to the file about to be read. Only consulted for \code{"auto"}.}
}
\value{
A single \code{TRUE} (keep it lazy) or \code{FALSE} (read it into memory).
}
\description{
Turns the user-facing \code{stream} argument into the single yes/no the readers
need. \code{"auto"} is the only mode that looks at the file at all; the other two
are the user overriding the guess in either direction.

\code{TRUE} and \code{FALSE} are accepted as aliases for \code{"always"} and \code{"never"},
because a logical is the first thing most callers will try.
}
\details{
The \code{"auto"} threshold is \code{getOption("DTAtools.stream_threshold")}, in bytes.

Note that the size compared is the size \emph{on disk}. For a compressed file --
\code{.csv.gz} -- that is the compressed size, which can be several times smaller
than what materialising it would cost, so \code{"auto"} under-triggers there. Pass
\code{stream = "always"} for a large compressed input.
}
\keyword{internal}
