% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reportRendering.R
\name{.report_with_metadata_row}
\alias{.report_with_metadata_row}
\title{Add the Metadata Axis to the Overview Rows}
\usage{
.report_with_metadata_row(x, results_df)
}
\arguments{
\item{x}{The object being reported on.}

\item{results_df}{The frame returned by \code{results(x)}.}
}
\value{
\code{results_df}, with one metadata row appended where warranted.
}
\description{
\code{results()} carries one row per dataset target and none for metadata: the
metadata verdict is computed by \code{check()} and kept on the object, never in
the frame \code{results()} returns. The overview is the first thing a reviewer
reads and for many it is the only thing, so a transfer that is invalid ONLY
because of a metadata import error must not headline as "everything passed"
while the fault sits in a message row further down the same page.

One row is added for the metadata axis when, and only when, it has something
to report. A clean transfer's overview stays a statement about its targets,
which is what the per-target table below the cards is for.
}
\keyword{internal}
