% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTAFile-class.R
\name{dta_check_readable_file}
\alias{dta_check_readable_file}
\title{The Guard Sequence Shared by \code{read_file()} and \code{open_file()}}
\usage{
dta_check_readable_file(x, file, namecheck, .caller)
}
\arguments{
\item{x}{A \code{DTAFile} object (or subclass).}

\item{file}{A character string specifying the path to the file.}

\item{namecheck}{Logical; when \code{TRUE} the file name must match the
object's filename or pattern.}

\item{.caller}{Name of the generic to blame in the single-path error
message (\code{"read_file"} or \code{"open_file"}).}
}
\value{
The validated \code{file} path.
}
\description{
Both generics accept the same arguments and must reject the same malformed
input the same way before handing off to their respective \verb{*_execution()}
method -- factored out here so the two stay in sync rather than drifting,
as two independent copies of the same checks eventually do.
}
\keyword{internal}
