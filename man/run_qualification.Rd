% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualification.R
\name{run_qualification}
\alias{run_qualification}
\title{Qualify the installed package on this system}
\usage{
run_qualification(
  output_dir,
  stages = c("IQ", "OQ", "PQ"),
  scale = c("full", "standard", "quick"),
  formats = c("md", "html", "docx", "pdf"),
  filter = NULL,
  tester = unname(Sys.info()[["user"]]),
  reviewer = NULL,
  include_unit_tests = c("auto", "never", "always"),
  perf_floor = NULL,
  seed = 20260101L,
  quiet = FALSE
)
}
\arguments{
\item{output_dir}{Character. Directory to create the evidence bundle in. A
run-specific subdirectory is created inside it; an existing bundle is
never overwritten.}

\item{stages}{Character vector of stages to run, any of \code{"IQ"}, \code{"OQ"} and
\code{"PQ"}. Pass \code{character()} to build the static parts of the bundle
(requirements, coverage, environment) without executing tests.}

\item{scale}{One of \code{"full"}, \code{"standard"} or \code{"quick"}. The tier controls
how much data the performance stage generates and how many randomised
cases the differential tests draw. \code{"full"} is the default because a
qualification run is deliberate and infrequent; \code{"quick"} exists for
development and continuous integration.}

\item{formats}{Character vector of report formats. Markdown is always
written. HTML and Word are produced when pandoc is available, PDF when a
PDF backend is (see \code{\link[=dta_pdf_backend]{dta_pdf_backend()}}); a format that cannot be produced
is recorded as such rather than failing the run.}

\item{filter}{Optional regular expression restricting which test files run.
A filtered run is marked partial, because it cannot support a complete
claim about the software.}

\item{tester}{Character. Who is performing the run. Recorded in the report.}

\item{reviewer}{Optional character. Who is reviewing it. Recorded in the
report's approval block.}

\item{include_unit_tests}{One of \code{"auto"}, \code{"never"} or \code{"always"}. Whether
to also run the developer test suite as supplementary evidence. It is only
reachable when the package was installed with its tests.}

\item{perf_floor}{Optional numeric. A throughput in rows per second that the
performance stage must reach. \code{NULL} records throughput without judging
it, which is the right default when the target hardware is unknown.}

\item{seed}{Integer. Seed for the randomised differential cases, so a run is
reproducible.}

\item{quiet}{Logical. Suppress the progress and summary output.}
}
\value{
Invisibly, an object of class \code{dta_qualification} with the verdict,
the per-test and per-expectation evidence, the traceability matrix, the
deviation register, the meta-consistency checks, the environment record
and the path to the bundle. Its \code{print()} method summarises the run.
}
\description{
Runs the qualification suite that ships with the package and writes an
evidence bundle: a Validation Summary Report, machine-readable results, a
requirements traceability matrix, a record of the environment, a
contemporaneous run log and a hash manifest covering all of it.
}
\details{
The suite is separate from the package's developer tests. It runs against
the \emph{installed} namespace, so what it reports is the behaviour of the copy
on this machine, and every step records what was expected alongside what
happened. It is meant to be run by hand when the package is deployed onto a
target system, and its output is meant to be reviewed and signed.

Three stages are executed, following the usual qualification vocabulary.
Installation qualification (\code{"IQ"}) records what is installed and checks it
against what should be. Operational qualification (\code{"OQ"}) exercises each
documented behaviour against explicit expected values. Performance
qualification (\code{"PQ"}) runs end-to-end workflows and measures behaviour at
scale.
}
\examples{
# A short installation-only run, which needs no test data.
bundle <- run_qualification(
  tempfile("qualification-"),
  stages = "IQ", scale = "quick", formats = "md", quiet = TRUE
)
bundle$verdict
}
\seealso{
\code{\link[=qualification_requirements]{qualification_requirements()}} for the requirements being verified.
}
