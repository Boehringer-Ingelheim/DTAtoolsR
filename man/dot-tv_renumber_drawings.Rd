% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateRegions.R
\name{.tv_renumber_drawings}
\alias{.tv_renumber_drawings}
\title{Give every drawing inside a region copy a fresh docPr id}
\usage{
.tv_renumber_drawings(node, doc)
}
\arguments{
\item{node}{The just-created copy (an \code{xml_node}), already attached to \code{doc}.}

\item{doc}{The whole document (\code{xml_document}), searched for the current
maximum id.}
}
\value{
Invisibly, \code{node}.
}
\description{
Word rejects a document with duplicate \verb{wp:docPr/@id} values. A region copy
made with \code{\link[xml2:xml_add_sibling]{xml2::xml_add_sibling()}} duplicates whatever ids the original
drawing (typically an inline image) carried, so each copy needs ids that do
not collide with any other \code{docPr} already in the document -- including
ids handed out to an earlier copy of the same region.
}
\details{
Only drawings are renumbered. A bookmark (\code{w:bookmarkStart} /
\code{w:bookmarkEnd}), footnote reference or comment reference inside a region
is copied verbatim, id included, in every repetition; renumbering those --
bookmarks in matched pairs, with unique names -- is a follow-up for the day
a template needs it.
}
\keyword{internal}
