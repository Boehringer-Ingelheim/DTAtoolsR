% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingValidation.R
\name{dta_schema_errors_stream}
\alias{dta_schema_errors_stream}
\title{Schema-Axis Validation over a Stream of Record Batches}
\usage{
dta_schema_errors_stream(specs, reader, max_errors = NULL)
}
\arguments{
\item{specs}{A \code{DTAColumnSpecCollection}.}

\item{reader}{An object with a \code{read_next_batch()} method -- an
\code{arrow::RecordBatchReader} or a \code{Scanner}'s reader.}

\item{max_errors}{Integer or \code{NULL}. Stop retaining individual errors once
this many have been collected. Counting continues regardless, so the
reported total is exact even when the retained detail is truncated. \code{NULL}
retains everything, matching the non-streaming path.}
}
\value{
A list with \code{summarised_error}, \code{full_error} and \code{n_errors}. When
retention was truncated, \code{full_error} carries a \code{truncated} attribute.
}
\description{
Evaluates the schema axis batch by batch, so peak memory is bounded by the
batch size rather than by the size of the input. Produces exactly the
\code{summarised_error} / \code{full_error} pair that \code{dta_schema_errors()} produces
for the same data.

Row numbers are global: each batch's errors are offset by the number of rows
already consumed, so a row number identifies a position in the input, not in
the batch it happened to fall in.
}
\keyword{internal}
