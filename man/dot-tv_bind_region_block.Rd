% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateRegions.R
\name{.tv_bind_region_block}
\alias{.tv_bind_region_block}
\title{Bind a bare dataset-block paragraph inside a region copy to one dataset}
\usage{
.tv_bind_region_block(p, name, nonce, k, variables)
}
\arguments{
\item{p}{An \code{xml_node} for a body paragraph -- already a copy, inside one
dataset's repetition of the region.}

\item{name}{Character. The current dataset's name.}

\item{nonce}{Character. This export's nonce, from \code{\link[=.tv_block_nonce]{.tv_block_nonce()}}.}

\item{k}{Integer. This region's running sentinel counter.}

\item{variables}{Named list of caller-supplied placeholder values.}
}
\value{
A named character vector of length 0 or 1: sentinel to
\code{"{NAME:dataset}"}.
}
\description{
A dataset block (\code{{COLUMN_SPECS}}, \code{{VALIDATION_RULES}}, \code{{FILE_SPECS}},
\code{{DATASETS}}/\code{{DATASETS_DETAIL}}) sitting alone in a paragraph of a region
copy is rewritten here to name the current dataset explicitly -- e.g.
\code{{COLUMN_SPECS}} becomes \code{{COLUMN_SPECS:demographics}} -- so
\code{\link[=.tv_render_blocks]{.tv_render_blocks()}} renders that one dataset instead of every dataset in
the DTA. An explicit \code{{COLUMN_SPECS:vitals}} is left untouched: the caller
already named a dataset, and rebinding it would silently override that
choice. A token the caller supplied a \code{variables} value for is left alone
too, for the same reason \code{\link[=.tv_mark_block_paragraphs]{.tv_mark_block_paragraphs()}} leaves one alone: a
user-supplied value wins over any block.
}
\details{
Uses its own sentinel family (\code{.tv_block_sentinel(nonce, paste0("R", k))})
so a region-bound block can never collide with a sentinel
\code{\link[=.tv_mark_block_paragraphs]{.tv_mark_block_paragraphs()}} stamps later, in the same document.
}
\keyword{internal}
