% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columnSpecChecks.R
\name{dta_report_columnspec_checks}
\alias{dta_report_columnspec_checks}
\title{Print the Per-Check Column Spec Report}
\usage{
dta_report_columnspec_checks(
  checks,
  unchecked_reason = "the scan did not evaluate it"
)
}
\arguments{
\item{checks}{A summary frame, from \code{dta_columnspec_check_summary()}.}

\item{unchecked_reason}{Character. Why a \code{not_checked} row could not be
settled, phrased to complete "... not checked: \if{html}{\out{<reason>}}".}
}
\value{
\code{NULL}, invisibly. Called for the console output.
}
\description{
Renders \code{details$columnspec_checks} to the console: one line per check kind
followed by a summary, mirroring how the rule axis reports one line per rule
followed by its own summary. Called by both the materialising and the
streaming path, so the two cannot report the same verdict differently.

Rows sharing a \code{check} label are folded into one line -- \code{enum} and \code{const}
are both the "values" check -- and a column declares at most one of them, so
the folded counts are sums rather than an over-count.
}
\keyword{internal}
