% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTAFileTabular-class.R
\name{dta_reader_na_values}
\alias{dta_reader_na_values}
\title{Missing-Value Markers for a Tabular Handler}
\usage{
dta_reader_na_values(x)
}
\arguments{
\item{x}{A \code{DTAFileTabular} object (or subclass).}
}
\value{
A character vector to pass as \code{na}, or \code{NULL} when the handler
declares no missing-value markers -- \code{NULL} lets the reader keep Arrow's
own default (\code{c("", "NA")}) rather than narrowing it.
}
\description{
Maps a \code{DTAFileTabular} handler's declared \code{missing_values} onto the \code{na}
argument the readers expect. An empty cell is always treated as missing
regardless of what a handler declares; anything in \code{missing_values} adds to
that rather than replacing it.
}
\keyword{internal}
