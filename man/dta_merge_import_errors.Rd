% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importConversion.R
\name{dta_merge_import_errors}
\alias{dta_merge_import_errors}
\title{Merge the Import-Time and Rule-Time Import Errors}
\usage{
dta_merge_import_errors(carried, rule_errors)
}
\arguments{
\item{carried}{A data.frame of import-time issues, or \code{NULL}.}

\item{rule_errors}{A data.frame of rule-time issues, or \code{NULL}.}
}
\value{
A data.frame in the shape of \code{\link[=dta_empty_import_errors]{dta_empty_import_errors()}}, carrying
the exact total in its \code{"n_import_errors"} attribute.
}
\description{
Combines the errors detected while typing the table with those the rule layer
found while reading a column as a number.

After the import choke point the two rarely overlap: a value the import layer
could not represent is already \code{NA} by the time a rule sees it, so the rule
layer reports it only for columns the import layer does not type (a \code{Char}
column, or one with no spec). Where they do overlap it is one error, not two.
}
\keyword{internal}
