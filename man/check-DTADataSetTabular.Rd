% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTADataSetTabular-class.R
\name{check-DTADataSetTabular}
\alias{check-DTADataSetTabular}
\title{Check DTADataSetTabular Tables}
\usage{
## S7 method for class <DTAtools::DTADataSetTabular>
check(
  x,
  tables = NULL,
  tab = NULL,
  force = FALSE,
  persist = TRUE,
  artifact_dir = NULL,
  quiet = FALSE,
  validation_run = NULL
)
}
\arguments{
\item{x}{A \'DTADataSetTabular\' object.}

\item{tables}{NULL (default), character table names, or numeric table indices.
If NULL and \code{tab} is also NULL, checks all tables.}

\item{tab}{Character table name or numeric table index (optional). If provided,
checks only this single table. Cannot be used together with \code{tables}.}

\item{force}{Logical. If TRUE, forces re-validation even if unchanged. Default is FALSE.}

\item{persist}{Logical. If TRUE (default), persists validation artifacts to disk.}

\item{artifact_dir}{Character or NULL. Optional output directory for persisted
validation artifacts.}

\item{quiet}{Logical. If TRUE, suppresses console output. Default is FALSE.}
}
\value{
Invisibly returns the updated \code{DTADataSetTabular} object \code{x},
with \code{validation_index}/\code{validation_store} updated and a
\code{"last_validation_summary"} attribute set. Use
\code{validation_status()} on the returned object to obtain the
validation summary data.frame.
}
\description{
Validates all tables or a specific table within a DTADataSetTabular object,
prints a validation summary to the console, and updates the object's
validation state.
}
\examples{
  ds <- create_example_DTADataSetTabular(2)
  # Check all tables
  ds <- check(ds)
  # Check specific table
  ds <- check(ds, tables = "tab1")
}
