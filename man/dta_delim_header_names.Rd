% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTAFileTabular-class.R
\name{dta_delim_header_names}
\alias{dta_delim_header_names}
\title{The Header Names of a Delimited File}
\usage{
dta_delim_header_names(path, parse_options, has_header, encoding = "UTF-8")
}
\arguments{
\item{path}{Character path to the delimited file.}

\item{parse_options}{An \code{arrow::CsvParseOptions}.}

\item{has_header}{Logical; whether the first line names the columns.}

\item{encoding}{The declared file encoding.}
}
\value{
A character vector of raw (uncleaned) column names.
}
\description{
Opens the file for its header alone. An \code{arrow::Dataset} reads its schema
from the first block and nothing else, so the cost is one block of I/O
whatever the file weighs: a median of 10 ms on a 169 MB CSV, against 5 ms on
a two-row one.

Arrow reports a header it cannot use -- two columns with the same name, most
commonly -- as a schema failure whose message does not mention the header at
all ("Could not read schema ... Is this a 'csv' file?"). It is re-raised here
naming the file and the likely cause, because this is the first point at
which the file is touched and therefore where the user will be looking.
}
\keyword{internal}
