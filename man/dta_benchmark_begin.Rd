% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking.R
\name{dta_benchmark_begin}
\alias{dta_benchmark_begin}
\title{Begin a Benchmark Measurement}
\usage{
dta_benchmark_begin(enabled)
}
\arguments{
\item{enabled}{Logical. Whether to begin a measurement.}
}
\value{
An opaque state list, or \code{NULL} when \code{enabled} is not \code{TRUE}, or
when a measurement is already in flight (see the nesting guard above).
}
\description{
Opens a benchmark measurement window, or declines to. Returns \code{NULL} --
rather than a state object -- whenever no measurement should be taken,
which lets a call site do \code{state <- dta_benchmark_begin(benchmark)} and
later \code{dta_benchmark_end(state)} unconditionally: the \code{NULL} propagates and
\code{dta_benchmark_end()} becomes a no-op.

\code{gc(reset = TRUE, full = TRUE)} runs \emph{before} the clock is read, so the
collection this call triggers is never charged to the measured region. See
\code{\link[=dta_benchmark_end]{dta_benchmark_end()}} for the matching rule at the other end.
}
\keyword{internal}
