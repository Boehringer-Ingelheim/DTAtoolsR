% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateDocx.R
\name{.tv_escape_regex}
\alias{.tv_escape_regex}
\title{Escape every regex metacharacter in a literal string}
\usage{
.tv_escape_regex(x)
}
\description{
Deliberately character-by-character rather than a regex of its own. Both
obvious alternatives are broken here: \verb{\\Q...\\E} quoting stops at the first
literal \verb{\\E} in a caller-supplied name, silently dropping that key from the
match set; and the usual
\verb{gsub("([\\\\^$.|?*+()\\\[\\\]\{\}])", ...)} spelling is itself a pattern with \code{{}}
inside a character class, which TRE rejects outright. This version has
neither failure mode, and \code{variables} names are arbitrary so neither can be
ruled out upstream.
}
\keyword{internal}
