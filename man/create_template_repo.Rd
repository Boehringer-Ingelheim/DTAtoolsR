% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createTemplateRepo.R
\name{create_template_repo}
\alias{create_template_repo}
\title{Scaffold a new private DTAtools template repository}
\usage{
create_template_repo(path, examples = TRUE, ci = TRUE, overwrite = FALSE)
}
\arguments{
\item{path}{A single non-empty character string: the directory to create
the repository in. Created recursively when it does not already exist.
Aborts via \code{\link[cli:cli_abort]{cli::cli_abort()}} if \code{path} names an existing file rather
than a directory.}

\item{examples}{A single \code{TRUE}/\code{FALSE} (default \code{TRUE}). When \code{TRUE},
copies the four starter template YAML files into \code{path}, alongside
\code{README.md}. When \code{FALSE}, only \code{README.md}, \code{.gitignore} and (if \code{ci}
asks for any provider) its CI file(s) are written -- no template files,
for a caller who wants the scaffolding without the worked example.}

\item{ci}{Either a single \code{TRUE}/\code{FALSE} (default \code{TRUE}), or a character
vector naming one or more of \code{"github"}, \code{"bitbucket"}, \code{"jenkins"}
(duplicates are silently dropped). \code{TRUE} is shorthand for \code{"github"}
only, matching this argument's original, still-default behaviour;
\code{FALSE} writes no CI file at all, and no \code{.github} directory. Each
provider writes one ready-to-run file that lints the repository with
\code{validate_template(".", strict = TRUE)}: \code{"github"} writes
\code{.github/workflows/validate-templates.yml} (GitHub Actions, triggered on
push and pull request); \code{"bitbucket"} writes \code{bitbucket-pipelines.yml}
(Bitbucket \strong{Cloud} Pipelines only -- Bitbucket Server / Data Center has
no Pipelines feature); \code{"jenkins"} writes a declarative \code{Jenkinsfile},
which is what a Bitbucket Server / Data Center deployment needs instead,
with its trigger configured on the Jenkins job rather than in the file.}

\item{overwrite}{A single \code{TRUE}/\code{FALSE} (default \code{FALSE}). When \code{FALSE},
this call aborts via \code{\link[cli:cli_abort]{cli::cli_abort()}}, naming every conflicting file,
and writes nothing at all, if any file it would write already exists at
\code{path} -- a partially-written repository would leave a caller unable to
tell which half of a family of cross-referencing templates they were
looking at. When \code{TRUE}, those files are replaced.}
}
\value{
Invisibly, the normalised absolute path to \code{path} (see
\code{\link[base:normalizePath]{base::normalizePath()}}).
}
\description{
Creates a directory laid out the way a private DTAtools template
repository is meant to be: a small, self-consistent worked example of the
four template kinds (a creation template, a dataset template, a party
profile, a controlled vocabulary), a \code{README.md} explaining them, a
\code{.gitignore}, and -- by default -- a GitHub Actions workflow that runs
\code{\link[=validate_template]{validate_template()}} against the repository on every push and pull
request. The result already passes \code{validate_template(path, strict = TRUE)} unmodified, so it is a starting point to edit rather than a stub to
fill in from nothing. See \code{vignette("private-templates", package = "DTAtools")} for how to point the bundled Shiny app at the directory this
creates.
}
\examples{
library(DTAtools)
dir <- file.path(tempdir(), "my-templates")
create_template_repo(dir)
list.files(dir, recursive = TRUE, all.files = TRUE, no.. = TRUE)
}
