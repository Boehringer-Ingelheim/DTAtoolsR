% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTAFile-class.R
\name{dta_print_file_count}
\alias{dta_print_file_count}
\title{Print a DTAFile's File-Count Bounds}
\usage{
dta_print_file_count(x)
}
\arguments{
\item{x}{A \code{DTAFile} object (or subclass).}
}
\value{
\code{NULL}, invisibly. Called for its printed side effect.
}
\description{
The number-of-files half of \code{print_info()}, factored out so \code{DTAFile} and
\code{DTAFileTabular} -- which cannot reach the parent method through
\code{S7::super()}, as this codebase uses it nowhere (see
\code{dta_matches_filename_base()}) -- apply the identical, NULL-safe rule
rather than two copies that can drift.

\code{min_number_of_files} and \code{max_number_of_files} are usually set together,
but a handler may declare only one bound (e.g. \code{min_number_of_files}
alone), leaving the other \code{NULL}. Comparing a number to \code{NULL} with \code{==}
yields a zero-length logical, and handing that to \code{if} aborts with
"argument is of length zero" -- this helper renders a \code{NULL} bound as
"unbounded" instead of ever making that comparison unguarded.
}
\keyword{internal}
