% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DTAFileTabular-class.R
\name{dta_read_delim_normalized}
\alias{dta_read_delim_normalized}
\title{Read a Delimited File Eagerly, With Clean Column Names}
\usage{
dta_read_delim_normalized(
  path,
  delim,
  quote,
  has_header,
  specs = NULL,
  na = NULL,
  handler = NULL
)
}
\arguments{
\item{path}{Character path to the delimited file.}

\item{delim, quote, has_header}{Delimited-text parse options.}

\item{specs}{A \code{DTAColumnSpecCollection} or \code{NULL}. When supplied, every
column is read as text; see \code{\link[=dta_delim_reader_plan]{dta_delim_reader_plan()}}.}

\item{na}{Character vector of strings to read as missing, or \code{NULL}
(the default) to keep Arrow's own default (\code{c("", "NA")}) unchanged.}

\item{handler}{A \code{DTAFileTabular} (or subclass) whose \code{newlines_in_values}
and \code{encoding} apply, or \code{NULL} for the defaults.}
}
\value{
An Arrow \code{Table}.
}
\description{
The eager half of \code{\link[=dta_delim_reader_plan]{dta_delim_reader_plan()}}: same names, same skip, same
column types as \code{\link[=dta_open_normalized_dataset]{dta_open_normalized_dataset()}} gives the scanner, read into
memory as an Arrow \code{Table} instead of left on disk.

A non-UTF-8 \code{encoding} is honoured here by Arrow itself: this reader owns
the input stream it reads through, and \code{csv_read_options(encoding = )}
re-encodes that stream. That is the one thing the lazy opener cannot do --
a dataset opens its own files -- which is why it converts the file to a
UTF-8 copy first (see \code{\link[=dta_transcode_to_utf8]{dta_transcode_to_utf8()}}) and reads that instead. Two
mechanisms, one result: the same bytes decode the same way whichever reader
loaded them.
}
\keyword{internal}
