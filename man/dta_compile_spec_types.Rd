% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importConversion.R
\name{dta_compile_spec_types}
\alias{dta_compile_spec_types}
\title{Compile Every Spec Column's Target Type Once}
\usage{
dta_compile_spec_types(specs)
}
\arguments{
\item{specs}{A \code{DTAColumnSpecCollection}, or \code{NULL}.}
}
\value{
A named character vector, keyed exactly as \code{\link[=dta_reader_col_types]{dta_reader_col_types()}}
derives its keys (\code{unique(c(names(columns), ids))}, with \code{NA}/\code{""} keys
dropped), with values from \code{\link[=dta_spec_r_type]{dta_spec_r_type()}}. \code{character(0)} when
\code{specs} is \code{NULL} or declares no columns.
}
\description{
A named character vector mapping every spec column key to the R type
\code{\link[=dta_spec_r_type]{dta_spec_r_type()}} would return for it, derived once per scan instead of
once per column per batch.

Mirrors \code{\link[=dta_compile_columnspec_schemas]{dta_compile_columnspec_schemas()}}, but for the coercion axis. A
column's target type is a pure function of its spec and does not change
while a table is being validated, but deriving it is several
\code{tryCatch()}/S7 hops deep (\code{dta_spec_column_structure()} walks the
collection to find the column's structure, then \code{as_r_type()} dispatches on
that structure). \code{\link[=dta_coerce_table_to_specs]{dta_coerce_table_to_specs()}} runs once per batch on the
streaming path, so looking the type up per column inside that call repeats
the whole derivation once per column per batch across a scan's thousands of
batches, to obtain one answer per column every time. Compiling the map once
and passing it in makes that cost proportional to the spec rather than to
the data.
}
\keyword{internal}
