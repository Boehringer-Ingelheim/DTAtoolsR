% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingValidation.R
\name{dta_validate_any_table}
\alias{dta_validate_any_table}
\title{Validate a Table However It Is Held}
\usage{
dta_validate_any_table(
  specs,
  table,
  verbose = FALSE,
  batch_rows = 131072L,
  max_errors = getOption("DTAtools.max_errors", 10000L),
  use_threads = TRUE,
  fail_fast = FALSE,
  on_missing_column = c("scan", "stop")
)
}
\arguments{
\item{specs}{A \code{DTAColumnSpecCollection}.}

\item{table}{An Arrow \code{Table}, \code{Dataset}, or reader.}

\item{verbose}{Logical. Print progress.}

\item{batch_rows}{Integer. Rows per batch when scanning.}

\item{max_errors}{Integer, or \code{NULL} to hold everything in memory. Cap on
the per-cell error detail held in RAM, defaulting to 10000 and
configurable via \code{options(DTAtools.max_errors = )}. The default is finite
because the error sinks hold one row per bad cell: on a large dirty file,
unbounded retention exhausts memory exactly as holding the data would.
Nothing is lost, though: rows past the cap spill to a session-temporary
directory and \code{\link[=collect_full_errors]{collect_full_errors()}} reassembles the complete detail;
counts and the pass/fail verdict are exact either way, and a frame whose
in-memory head is incomplete is flagged as such.

It applies to a table already in memory as well. There is no spill on that
path -- the whole table is present, so validating again with a larger cap
recovers the dropped rows -- but the counts are exact there for the same
reason they are here: only retention is capped.}

\item{use_threads}{Logical. Whether Arrow's Scanner should use multiple
threads for I/O and decompression. Arrow buffers batches ahead of R in its
own C++ pool, outside the R heap, so single-threaded scanning is the lever
when RSS rather than speed is the constraint.}

\item{fail_fast}{Logical. Stop at the first batch that shows any problem
instead of scanning to the end. The report is then explicitly incomplete
and carries a \code{partial_scan} attribute. Ignored for a table already held in
memory, which is validated in one pass.}

\item{on_missing_column}{One of \code{"scan"} or \code{"stop"}. \code{"scan"}, the default,
preserves existing behaviour: a column the specs require but the table
lacks is reported once per row. \code{"stop"} decides it from the column names
alone and reads nothing.

Note this gate runs ahead of the lazy/in-memory dispatch, so unlike
\code{fail_fast} it applies to a materialised table as well -- the column names
are available whatever the holding. When they cannot be obtained without
consuming the table, the gate is skipped and the scan proceeds as usual.}
}
\value{
A validation details list.
}
\description{
Dispatches to the streaming path for a lazy table and the materialising path
for one already in memory, returning the same details either way. This is
what lets \code{check()} accept both without its callers knowing which they have.
}
\keyword{internal}
