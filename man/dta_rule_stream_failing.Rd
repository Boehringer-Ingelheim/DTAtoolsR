% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingValidation.R
\name{dta_rule_stream_failing}
\alias{dta_rule_stream_failing}
\title{Whether a Partial Scan Can Already Call a Rule Failed}
\usage{
dta_rule_stream_failing(state)
}
\arguments{
\item{state}{An accumulator from \code{dta_rule_stream_init()}.}
}
\value{
A single logical.
}
\description{
The predicate behind \code{fail_fast}. A rule counts as failed only when no later
batch could overturn the verdict.

\code{state$count} alone is not that predicate. A grouped rule never increments
it -- its verdict is reached in \code{dta_group_stream_finalise()}, after the
scan -- so reading \code{count} left \code{fail_fast} scanning a whole file whose very
first rows already broke a grouped constraint. Unsupported and
not-applicable rules are failures the moment they are discovered, and are
likewise invisible in \code{count}.
}
\keyword{internal}
