% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streamingValidation.R
\name{dta_row_key}
\alias{dta_row_key}
\title{Collision-Free Row Key for a Set of Columns}
\usage{
dta_row_key(df, cols)
}
\arguments{
\item{df}{A data frame.}

\item{cols}{Character. The key columns.}
}
\value{
A character vector, one key per row.
}
\description{
One string per row, equal for two rows exactly when \code{duplicated()} would
call those rows duplicates, so that keys can stand in for the rows
themselves -- in a hash set across batches, or in \code{duplicated()} on a single
vector instead of on a data frame.

The encoding is injective, which a plain separator-joined key is not:
\code{c("x", "y\\037z")} and \code{c("x\\037y", "z")} are different rows that would both
render to \verb{x\\037y\\037z}, silently merging distinct keys. Within a field a
reserved byte is therefore escaped -- \code{ESC} becomes \verb{ESC e}, the separator
becomes \verb{ESC s} -- so an encoded field can contain neither a bare separator
nor a bare \code{ESC}, and splitting the joined string on the separator recovers
the fields exactly.

Missing values are given the marker \verb{ESC n}, which no literal can produce
(a literal \verb{ESC n} encodes to \verb{ESC e n}). They therefore compare equal to
each other and to nothing else, which is \code{duplicated()}'s notion, under
which repeated NAs are duplicates but \code{NaN} is not one of them.

Doubles are rendered with \verb{\%.17g} rather than through \code{as.character()},
which rounds to 15 significant digits: \code{0.1 + 0.2} and \code{0.3} are two
different doubles that \code{duplicated()} keeps apart and \code{as.character()} does
not. That applies to everything stored as a double, \code{POSIXct} and \code{Date}
included, so a sub-second timestamp keys exactly, and to both parts of a
\code{complex}. \code{integer64} is excluded: it is stored as a double but is not one,
and bit64 renders it exactly itself.

What is left is as distinguishable as its character rendering -- a list
column, or any class with its own \code{as.character()} method -- which is why
the eager path in \code{dta_count_duplicates()} keeps its own type gate rather
than trusting every type to this function.

The escaping pass is skipped when the column holds no reserved byte at all,
which is the overwhelmingly common case. That is a pure optimisation and not
a second encoding: \code{gsub()} is the identity on text it does not match, so
the result is the same string either way, and equal rows in different
batches still encode equally -- which is what the streaming path's key set
depends on.
}
\keyword{internal}
