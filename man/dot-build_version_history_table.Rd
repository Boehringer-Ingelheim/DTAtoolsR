% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documentBuilders.R
\name{.build_version_history_table}
\alias{.build_version_history_table}
\title{Build the version-history table (flextable). Shared by the built-in layout and
by the \code{{VERSION_HISTORY_TABLE}} template block.}
\usage{
.build_version_history_table(version_history)
}
\arguments{
\item{version_history}{A list of \code{list(version =, date =, changes =)} records,
as \code{DTAMetaData@version_history} holds them, or \code{NULL}.}
}
\value{
A flextable, or \code{NULL} when there is no history.
}
\description{
\code{changes} is flattened rather than passed straight into the data.frame: the
class permits a character vector there, and a vector of length > 1 makes
\code{data.frame()} abort with "arguments imply differing number of rows". The
date goes through \code{\link[=.format_document_date]{.format_document_date()}} so it reads the same whatever the
machine's locale, and whether it arrives as a \code{Date} or as text.
}
\keyword{internal}
