% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportValidationReport.R
\name{write_validation_report}
\alias{write_validation_report}
\title{Export a Standalone HTML Validation Report}
\usage{
write_validation_report(
  x,
  file,
  max_repeats = 5,
  overwrite = FALSE,
  quiet = FALSE,
  title = NULL
)
}
\arguments{
\item{x}{A \code{DTA} object, already validated via \code{\link[=check]{check()}}.}

\item{file}{Character. Output \code{.html} file path.}

\item{max_repeats}{Positive integer (default \code{5}). Maximum number of
repeated identical messages shown before the rest collapse behind a
"show more" toggle in the report. Pass \code{NULL} or \code{Inf} to disable
capping and always show every message.}

\item{overwrite}{Logical. Whether to overwrite an existing file at \code{file}.
Default \code{FALSE}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses the success message. Default
\code{FALSE}.}

\item{title}{Optional character scalar used as the report's page title
and heading. Defaults to the DTA's metadata title (\code{metadata(x)@title})
when available, else \code{"Validation Report"}.}
}
\value{
Invisibly returns \code{file}.
}
\description{
Renders a self-contained HTML file (no external assets) summarizing
validation results for a \code{DTA} object: a pass/fail overview per
dataset/target, and a sortable, filterable table of every validation
message with click-to-inspect detail. Repeated identical messages (same
dataset/target/source/rule/column/keyword/message, differing only by row)
are capped at \code{max_repeats} in the default view with a "show more" toggle
in the resulting HTML.
}
\examples{
dta <- create_example_DTA()
dta <- check(dta, quiet = TRUE)
out <- tempfile(fileext = ".html")
write_validation_report(dta, out)
}
