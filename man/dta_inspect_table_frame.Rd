% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validationReporting.R
\name{dta_inspect_table_frame}
\alias{dta_inspect_table_frame}
\title{Table Frame for Inspection, Bounded and Column-Limited}
\usage{
dta_inspect_table_frame(tbl, columns = NULL, max_row = NULL)
}
\arguments{
\item{tbl}{A table representation, as held in \code{x@tables[[table_name]]}.}

\item{columns}{Character, or \code{NULL} for every column.}

\item{max_row}{Numeric, or \code{NULL}/non-finite for no row bound. When finite
and positive, only the first \code{max_row} rows are read, so row \code{k} of the
returned frame stays row \code{k} of the underlying table.}
}
\value{
A data.frame, or \code{NULL} when \code{tbl} cannot be safely re-read.
}
\description{
\code{dta_inspect_tabular_message()} used to call
\code{as.data.frame(x@tables[[table_name]])} unconditionally, which for a
streamed table materialises the whole delivery -- everything \code{check()} was
streaming specifically to avoid -- just to show one row of context. This
collects only what a preview actually needs: a column subset and,
when supplied, the first \code{max_row} rows.

A table already in memory is untouched: \code{as.data.frame()}, exactly as
before. A \code{RecordBatchReader} cannot be safely re-read -- it is one-shot,
and the scan \code{check()} ran may already have consumed it -- so this returns
\code{NULL} rather than reading (and thereby destroying) whatever is left of it.
Any other lazy source (\code{Dataset}, \code{arrow_dplyr_query}) is scanned fresh via
\code{dplyr}, which is safe to repeat.
}
\keyword{internal}
