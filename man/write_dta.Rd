% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportDocuments.R
\name{write_dta}
\alias{write_dta}
\title{Export DTA Metadata as Professional Document}
\usage{
write_dta(
  x,
  file,
  format = NULL,
  overwrite = FALSE,
  include_signatures = TRUE,
  signature_list = NULL,
  quiet = FALSE,
  include_yaml = FALSE,
  yaml_text = NULL,
  template = NULL,
  template_variables = NULL
)
}
\arguments{
\item{x}{A DTA object to export}

\item{file}{Character. Output file path (with .docx, .pdf, or .md extension)}

\item{format}{Character. Output format: "docx" (default), "pdf", or "md".
If NULL, inferred from file extension.}

\item{overwrite}{Logical. Whether to overwrite existing files. Default: FALSE.}

\item{include_signatures}{Logical. Whether to include signature approval section. Default: TRUE.}

\item{signature_list}{List of signatories with name and role fields.}

\item{quiet}{Logical. If TRUE, suppresses console output. Default is FALSE.}

\item{include_yaml}{Logical. If \code{TRUE}, append the machine-readable YAML
specification as a final, small-font section of the built-in DOCX/PDF
layout. Requires \code{yaml_text}. Ignored -- with a warning -- for
\code{format = "md"}, when a \code{template} is supplied, and when \code{yaml_text} is
\code{NULL} or empty. Default: \code{FALSE}.}

\item{yaml_text}{Optional character scalar holding the YAML specification to
embed when \code{include_yaml = TRUE}. Default: \code{NULL}.}

\item{template}{Optional character path to a Word (\code{.docx}) template
containing placeholder markers such as \code{{DTA_TITLE}}. When supplied, the
document is produced by filling the template via \code{\link[=export_with_template]{export_with_template()}}
instead of the built-in layout. Only valid for \code{format = "docx"} or
\code{"pdf"}. Default: \code{NULL}.}

\item{template_variables}{Optional named list of additional or overriding
placeholder values passed to \code{\link[=export_with_template]{export_with_template()}} when \code{template} is
supplied. Default: \code{NULL}.}
}
\value{
Invisibly returns the document object (for chaining), or the output
file path when a \code{template} is used.
}
\description{
Creates a professionally formatted DOCX, PDF, or Markdown document from a DTA object,
including metadata, version history, transmission details, and approval signatures.
}
\examples{
\dontrun{
dta <- DTA(
  title = "Clinical Data Transfer",
  version = "1.0",
  date = Sys.Date()
)
write_dta(dta, file = "dta_metadata.docx")

# Fill a user-authored Word template instead of the built-in layout
write_dta(dta, file = "dta_from_template.docx", template = "my_template.docx")
}
}
\seealso{
\code{\link[=export_with_template]{export_with_template()}} for the full list of supported placeholders.
}
