% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columnSpecChecks.R
\name{dta_compile_columnspec_schemas}
\alias{dta_compile_columnspec_schemas}
\title{Compile a Collection's Column Schemas Once}
\usage{
dta_compile_columnspec_schemas(specs)
}
\arguments{
\item{specs}{A \code{DTAColumnSpecCollection}.}
}
\value{
A list with one element per column, each a list of \code{name} (the
expected column name) and \code{schema} (the column's schema, or \code{NULL} when it
could not be derived).
}
\description{
Derives the \code{as_json_schema()} form of every column in a collection, paired
with the name that column is expected to carry in the data.

A column's schema is a pure function of its \code{DTAColumnSpec}, which does not
change while a table is being validated. Deriving it is nevertheless several
S7 dispatches deep -- \code{as_json_schema()} calls \code{as_json_schema_type()}, which
dispatches again on the structure -- so on the streaming path, where
\code{dta_columnspec_errors()} is called once per batch, deriving it inside that
call repeated the whole derivation once per column per batch. A 500-column
spec scanned in 1000 batches paid for it 500,000 times to obtain 500 distinct
answers.

Compiling once and passing the result in makes that cost proportional to the
spec rather than to the data.
}
\keyword{internal}
