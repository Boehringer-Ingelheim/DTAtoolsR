% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validateTemplate.R
\name{validate_template}
\alias{validate_template}
\title{Validate DTA templates against the template engine's own rules}
\usage{
validate_template(path, strict = FALSE, kinds = NULL)
}
\arguments{
\item{path}{A single template file, or a directory. A directory is scanned
NON-RECURSIVELY for \verb{*.dta-template.yaml}, \verb{*.dta-dataset-template.yaml}
and \verb{*.dta-party.yaml} files (and their \code{.yml} spellings) -- the same
filename convention the template engine itself uses
(\code{dta_template_kind_pattern()}). \code{kinds}, below, only narrows which of
those files get a report row of their OWN; it never changes how many
files are considered when resolving a cross-file reference (an
\verb{extends:}, a \verb{datasets[].template}, a party slot's profile allow-list).}

\item{strict}{A single \code{TRUE}/\code{FALSE}. When \code{TRUE}, \code{validate_template()}
raises an error via \code{\link[cli:cli_abort]{cli::cli_abort()}} summarising every row of severity
\code{"error"} (and stays silent when there are none), instead of just
returning them -- the one line a CI job needs.}

\item{kinds}{\code{NULL} (the default: every kind) or a character vector naming
one or more of \code{"dta_creation_template"}, \code{"dta_dataset_template"},
\code{"dta_party_profile"}. Restricts which files in a scanned directory get
their own report row; has no effect when \code{path} is a single file (an
explicitly named file is always checked).}
}
\value{
A data frame (\code{stringsAsFactors = FALSE}) with one row per issue
found, and columns \code{file}, \code{kind}, \code{id}, \code{version}, \code{severity}
(\code{"error"} or \code{"warning"}), \code{code}, \code{message}. Zero rows means every
checked file passed every check.
}
\description{
Runs the structural checks the bundled Shiny app's "Create new from
template" picker relies on -- unresolvable \verb{extends:}, an option target
that does not resolve to a real metadata field, a party slot naming a
profile that does not exist, a dataset patch naming an absent column, and
(as a final, comprehensive check) actually building a DTA from every
non-abstract creation template with its own default selections -- WITHOUT
starting the app. This lets a template repository (kept private, on
Bitbucket Data Center in production and mirrored to GitHub for CI) run its
own continuous integration against the templates it authors, with nothing
more than an R installation of DTAtools. See
\code{system.file("extdata", "templates", "validate-templates.yml", package = "DTAtools")} for a ready-to-copy GitHub Actions workflow.
}
\examples{
library(DTAtools)
issues <- validate_template(system.file("extdata", "templates", package = "DTAtools"))
issues
}
