% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateBlocks.R
\name{.tv_scan_block_tokens}
\alias{.tv_scan_block_tokens}
\title{Every block token still present in a finished .docx}
\usage{
.tv_scan_block_tokens(docx_path)
}
\value{
Character vector of unique tokens exactly as they appear.
}
\description{
Run over the \emph{output}, after rendering, to find what could not be rendered:
a token inside a sentence, inside a table cell, in a header or a footer, or
one put back because its \verb{:DATASET} argument named nothing. It reports rather
than drives -- what gets rendered is decided before substitution, by
\code{\link[=.tv_mark_block_paragraphs]{.tv_mark_block_paragraphs()}}.
}
\details{
Scans the paragraph \emph{text} of \code{word/document.xml} and every header/footer
part, not the raw XML: Word freely splits a typed placeholder across runs, so
a raw grep would miss a token the exporter itself handles perfectly well.
Also collects region markers (\verb{\{#DATASETS\}} / \verb{\{/DATASETS\}}) left behind by
a malformed region -- unclosed, unopened, or not alone in its paragraph.
}
\keyword{internal}
