% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateRules.R
\name{dta_numeric_raw}
\alias{dta_numeric_raw}
\title{The Source Text Behind a Strict Numeric Conversion}
\usage{
dta_numeric_raw(entry, idx)
}
\arguments{
\item{entry}{A list returned by \code{\link[=dta_as_numeric_strict]{dta_as_numeric_strict()}}.}

\item{idx}{An index vector into the column.}
}
\value{
A character vector of the source text at \code{idx}.
}
\description{
Returns the verbatim source text of selected rows of a
\code{\link[=dta_as_numeric_strict]{dta_as_numeric_strict()}} result.

The conversion keeps the \emph{source vector} rather than a character rendering of
the whole column, because the text is only ever read at the handful of rows
that failed to convert -- usually none. On a 200,000-row numeric column the
eager character copy was 13 MB against a 1.5 MB column, and one was held per
cached column simultaneously.

Rendering at the index is exactly equivalent to indexing an eagerly rendered
vector, branch by branch:
\itemize{
\item numeric/logical: the text was \code{as.character(x)}, and \code{as.character()} is
elementwise, so \code{as.character(x[i])} is the same string.
\item factor: the text was \code{as.character(x)}, which yields the \emph{labels}, not the
integer level codes. \code{x[i]} on a factor is still a factor carrying its
levels, so \code{as.character(x[i])} is the same label.
\item character: the text was the \strong{untrimmed} \code{as.character(x)}. \code{source} is
the untrimmed column, so this must stay untrimmed here too -- the trimming
applies to parsing only.
\item Date/POSIXt: the text was \code{as.character(x)}; \code{[} preserves the class and
the time zone, so formatting at the index is unchanged.
}
}
\keyword{internal}
