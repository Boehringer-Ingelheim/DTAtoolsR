% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateSchemaRules.R
\name{rule_check_condition}
\alias{rule_check_condition}
\title{Rule: check_condition}
\usage{
rule_check_condition(rule, df)
}
\arguments{
\item{rule}{A DTARule object of type \code{"check_condition"}. Expected slots:
\itemize{
\item \verb{@id} character
\item \verb{@type} = "check_condition"
\item \verb{@condition} list: named by column, each with one of:
\code{equals}, \code{not_equals}, \verb{in}, \code{not_in},
\code{greater}, \code{less}, \code{greater_equal}, \code{less_equal}, \code{range}, \code{empty}
\item \verb{@then} list: same structure as \verb{@condition}
}}

\item{df}{A data.frame to validate.}
}
\value{
A list with elements \code{id}, \code{valid}, and \code{message}.
}
\description{
Evaluates an \strong{IF/THEN} rule across rows:
If all \verb{@condition} predicates are TRUE for a row, then all \verb{@then}
predicates must also be TRUE. For rows where the IF holds, \code{NA} in THEN
is considered a \strong{violation}.
}
\details{
Supported operators per column (single operator per column):
\itemize{
\item Equality: \code{equals}, \code{not_equals}
\item Set: \verb{in}, \code{not_in}
\item Numeric comparisons: \code{greater}, \code{less}, \code{greater_equal}, \code{less_equal}, \code{range}
\item Emptiness: \code{empty} (TRUE means empty: \code{NA}, \code{NaN}, or \code{""}; FALSE means not empty)
}

If \verb{@condition} is empty, the \verb{@then} part applies to \strong{all rows}.
}
\examples{
# Example: If species == "setosa", then petal_length in [1.0, 1.9]
# rule_check_condition(rule, iris)
}
