% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateRegions.R
\name{.tv_expand_regions}
\alias{.tv_expand_regions}
\title{Expand \verb{\{#DATASETS\}} ... \verb{\{/DATASETS\}} repeating regions in a document part}
\usage{
.tv_expand_regions(xml_path, dta, nonce, variables = list())
}
\arguments{
\item{xml_path}{Character. Path to the template's \code{word/document.xml}.}

\item{dta}{A \link{DTA} object.}

\item{nonce}{Character. This export's nonce, from \code{\link[=.tv_block_nonce]{.tv_block_nonce()}}.}

\item{variables}{Named list of caller-supplied placeholder values.}
}
\value{
A named character vector, sentinel to \code{"{NAME:dataset}"}, for every
dataset block a region copy bound -- to be added to the \code{blocks}
argument of \code{\link[=.tv_render_blocks]{.tv_render_blocks()}}. Empty when nothing was expanded.
}
\description{
Runs as "pass 0", before \code{\link[=.tv_mark_block_paragraphs]{.tv_mark_block_paragraphs()}}, directly on the
template's \code{word/document.xml}. A region is opened by a top-level body
paragraph whose whole (trimmed) text is \verb{\{#DATASETS\}} and closed by the
next top-level body paragraph whose whole text is \verb{\{/DATASETS\}}; every body
child strictly between the two -- paragraph or table -- is copied once per
dataset in \code{names(dta@datasets)}, in document order, with the six
per-dataset placeholders (\code{\link[=.tv_dataset_variables]{.tv_dataset_variables()}}) resolved inside each
copy and a bare dataset block bound to that dataset
(\code{\link[=.tv_bind_region_block]{.tv_bind_region_block()}}). The two marker paragraphs and the original
region content are then removed, so zero datasets makes the whole region
vanish.
}
\details{
This must run before block-paragraph stamping: a bound dataset block
inside a region needs its own paragraph -- freshly copied per dataset --
stamped with its own sentinel; stamping the template's un-copied paragraph
first would apply to a paragraph this pass is about to multiply or delete.

Regions do not nest. An inner \verb{\{#DATASETS\}} paragraph is ordinary region
content: it is copied, left as literal text, and reported by the "block
placeholders were left unchanged" warning like any other malformed marker
(an unclosed open, an unopened close, or a marker not alone in a
paragraph) -- this function only ever pairs an open marker with the FIRST
close marker after it.
}
\keyword{internal}
