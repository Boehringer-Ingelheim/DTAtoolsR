% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_helpers.R
\name{dta_group_label_value}
\alias{dta_group_label_value}
\title{Render a Group-Label Value}
\usage{
dta_group_label_value(x)
}
\arguments{
\item{x}{A single group-column value.}
}
\value{
A length-1 character string.
}
\description{
Group-condition messages render each grouping column's value with
\code{as.character()}, e.g. \code{"AGE=1000000"}. For a numeric column that used to
show \code{"1e+06"} where its integer twin showed \code{"1000000"} -- and which one
you got depended on the int-vs-double narrowing decision, which
legitimately differs between the streamed (per-batch) and eager
(whole-table) paths. \code{digits = 15} mirrors \code{as.character()}'s precision,
so the rendering matches \code{as.character()} wherever that form is
non-scientific; a value whose \code{as.character()} form IS scientific (very
small or very large magnitudes) expands to plain digits instead, which can
be long for extreme values -- identical on both paths either way.
}
\keyword{internal}
