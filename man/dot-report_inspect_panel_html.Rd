% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reportRendering.R
\name{.report_inspect_panel_html}
\alias{.report_inspect_panel_html}
\title{Build Inspection Detail Panel HTML}
\usage{
.report_inspect_panel_html(inspect_rows, rule_def = NULL)
}
\arguments{
\item{inspect_rows}{A data.frame with ONE OR MORE rows, all sharing the
same message \code{id}, as returned by
\code{DTAtools::inspect(dta, id = id, as_tibble = FALSE)}. \code{inspect()} can
legitimately return more than one row per id (a rule's failing-row
preview, or multiple group_condition violations); every row is used
where it matters (the "actual" tables), while metadata that's constant
across rows (\code{id}, \code{type}, \code{message}, \code{headline}) is read from the first.}

\item{rule_def}{Optional S7 rule object to describe; \code{NULL} for
columnspec/import messages, or when the rule definition could not be
resolved.}
}
\value{
A length-1 character string containing the HTML detail panel.
}
\description{
Renders a hidden detail panel for one validation message: a plain-language
"should be" vs "actual" comparison, a "What this rule checks" box for rule
violations, and the raw technical detail in a collapsible section.
}
\keyword{internal}
