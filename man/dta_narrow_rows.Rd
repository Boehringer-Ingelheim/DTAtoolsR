% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_helpers.R
\name{dta_narrow_rows}
\alias{dta_narrow_rows}
\title{Narrow Reported Row Numbers Back to Integer}
\usage{
dta_narrow_rows(v)
}
\arguments{
\item{v}{A numeric vector of row numbers.}
}
\value{
\code{v} as an integer vector when every value fits the integer range, and
\code{v} unchanged otherwise.
}
\description{
The streaming driver turns a batch-local row number into a global one by
adding the number of rows already consumed. That offset is a double, for the
reason given above \code{dta_narrow_count()}: an integer offset silently becomes
\code{NA} past \code{.Machine$integer.max}, and every reported row number goes with it,
on exactly the files the streaming path exists to handle.

Adding a double to an integer vector widens the whole vector, so without this
the reported \code{row} column would change type for every file, however small.
Integer is what every consumer of the error frames has always seen, so the
vector is narrowed back whenever it still fits.

The range test is two-sided, mirroring \code{dta_narrow_count()}: a one-sided
\code{v <= .Machine$integer.max} is true of every negative number, so it would
hand \code{as.integer()} precisely the values it cannot represent. \code{NA} passes the
test because \code{as.integer(NA)} is \code{NA_integer_} and loses nothing; a row
number genuinely beyond the integer range stays a double rather than becoming
\code{NA}.
}
\keyword{internal}
