% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importConversion.R
\name{dta_coerce_column}
\alias{dta_coerce_column}
\title{Coerce One Column to Its Declared R Type}
\usage{
dta_coerce_column(values, target)
}
\arguments{
\item{values}{A column vector taken from the table.}

\item{target}{Character. The target R type, from \code{\link[=as_r_type]{as_r_type()}}.}
}
\value{
\code{NULL} when the column is left untouched, otherwise a list with the
converted \code{values}, the integer indices of the \code{offending} values, and
\code{source} -- the original input vector, from which the raw text of row \code{i}
is \code{as.character(source[i])}, i.e. \code{\link[=dta_numeric_raw]{dta_numeric_raw()}}.
}
\description{
Converts a single column to the type its spec declares, reporting the values
that could not be represented.

Only numeric targets are converted. Everything else -- \code{"character"} in
particular -- is returned untouched, which is what keeps a \verb{SAS Char}
\code{SUBJECT_ID} of \code{"007"} from ever being round-tripped through a number.

A column that is already numeric is left alone: there is nothing to parse, so
no value can fail to parse. This is also what stops the import axis from
inventing errors on a clean file.

An \code{Int} target is narrowed to R \code{integer} only when every value is whole.
Rounding a fractional value into an integer column would silently discard the
fraction \emph{and} hide the \code{type: integer} column spec error that exists to report
it, so a fractional value stays a double and is left to the column spec axis.
}
\keyword{internal}
