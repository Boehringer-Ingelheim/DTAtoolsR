% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importConversion.R
\name{dta_coerce_table_to_specs}
\alias{dta_coerce_table_to_specs}
\title{Type a Table by Its Column Specs at Import Time}
\usage{
dta_coerce_table_to_specs(table, specs)
}
\arguments{
\item{table}{An Arrow Table or a data.frame.}

\item{specs}{A \code{DTAColumnSpecCollection}, or \code{NULL}.}
}
\value{
A list with \code{table} (the typed table, same class as the input, with
the issues attached) and \code{issues} (a data.frame in the shape of
\code{\link[=dta_empty_import_errors]{dta_empty_import_errors()}}, carrying the exact error count in its
\code{"n_import_errors"} attribute).
}
\description{
The typed import choke point. The declared type of every column is applied to
the data as it is read, instead of leaving the reader to infer a type per
column.

Arrow infers the type of a CSV/TSV column from its contents, so a single
unparseable cell in an otherwise numeric column makes the reader fall back to
string for \emph{every} row of that column. Every downstream check then sees a
column of text where the specification declared a number. Applying the
declared type here means the column is a number, one cell is missing, and
that one cell is reported.

Semantics:
\itemize{
\item A value that cannot be represented in the declared type becomes \code{NA} in the
typed column, and its source text is retained verbatim in an import issue
with \code{reason = "not_convertible"}.
\item Only unrecoverable values are issues. \code{"007"} to \code{7} and \code{"1.50"} to \code{1.5}
are clean conversions and are not reported.
\item A column whose declared type is \code{Char} is never coerced.
\item A column present in the table but absent from the specs is left untouched,
not dropped.
\item \code{NA}, \code{""} and whitespace in the source stay missing and are not issues.
}

The issues are returned \emph{and} attached to the returned table as the
\code{"dta_import_issues"} attribute. The carried copy is deliberate:
\code{\link[=check]{check()}} skips revalidation when the table hash and the specs hash are both
unchanged, and that hash is taken from the table itself. Issues living only
in the dataset's \code{import_issues} property would not be hashed, so a table
whose import issues had changed could be skipped while still reporting a
stale \code{ok = TRUE}. Riding on the table, they cannot be separated from the
data they describe.
}
\keyword{internal}
