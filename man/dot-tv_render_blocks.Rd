% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportTemplateBlocks.R
\name{.tv_render_blocks}
\alias{.tv_render_blocks}
\title{Replace the stamped block-placeholder paragraphs with rendered Word content}
\usage{
.tv_render_blocks(docx_path, dta, blocks = character(0), quiet = FALSE)
}
\arguments{
\item{docx_path}{Character. The .docx produced by pass 1; rewritten in place.}

\item{dta}{A \link{DTA} object.}

\item{blocks}{Named character vector from \code{\link[=.tv_mark_block_paragraphs]{.tv_mark_block_paragraphs()}}:
sentinel to the token it replaced.}

\item{quiet}{Logical. Suppress the warning about placeholders left behind.}
}
\value{
Invisibly, \code{docx_path}.
}
\description{
Runs only when \code{\link[=.tv_mark_block_paragraphs]{.tv_mark_block_paragraphs()}} stamped something, so a template
built from inline placeholders alone is never re-serialised by officer.
}
\details{
Each sentinel is unique and occupies exactly one paragraph, so the cursor
lands on the paragraph the \emph{template} marked -- never on one that merely
reads like a placeholder because a substituted value spelled one.
}
\keyword{internal}
