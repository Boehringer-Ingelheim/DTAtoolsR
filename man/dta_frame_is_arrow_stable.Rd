% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importConversion.R
\name{dta_frame_is_arrow_stable}
\alias{dta_frame_is_arrow_stable}
\title{Would This Frame Come Back Unchanged From Arrow?}
\usage{
dta_frame_is_arrow_stable(df)
}
\arguments{
\item{df}{A data.frame, or any other object (which yields \code{FALSE}).}
}
\value{
\code{TRUE} or \code{FALSE}. A frame with no columns is stable.
}
\description{
\code{TRUE} when every column of \code{df} is of a type that
\code{as.data.frame(arrow::as_arrow_table(df))} returns identically, so that a
digest taken from the frame is also the digest of the Arrow table built from
it.

The \code{DTADataSetTabular()} constructor stamps a data-frame table with
\code{\link[=dta_table_content_hash]{dta_table_content_hash()}} of the frame it coerced, rather than of the Table
it then builds -- which is what lets it convert the data once instead of
three times. That is only the same answer while the round trip is lossless,
and for three types it is not:
\itemize{
\item a \code{bit64::integer64} whose values fit in 32 bits comes back as \code{integer}
(Arrow's \code{int64_downcast});
\item a \code{difftime} comes back in seconds whatever units it went in as;
\item a \code{POSIXct} carrying \code{tzone = ""} -- what \code{as.POSIXct()} leaves when no
timezone is named -- comes back with no \code{tzone} at all, and after a second
round trip with the session's timezone. That one is not stable under
REPEATED round trips either, so the route this predicate sends it down
only helps while the coercion has nothing to type: when the coercion
rebuilds the table, its stamp is the digest of the first round trip and
\code{as.data.frame()} of the stored Table is the second. Such a table is
rescanned whenever it is rebuilt from its own contents, on either route.
It is pinned in \code{test-DTADataSetTabular-validation.R} rather than fixed;
the fix would be Arrow's.
\item a \code{Date} or a \code{POSIXct} held over INTEGER storage -- which
\code{structure(18262L, class = "Date")} and \code{.POSIXct(1L, "UTC")} are, and
which nothing in R forbids -- comes back over double storage, because Arrow
has one date32/timestamp type and R's converter builds a double from it.
The class is unchanged and the values are equal, but \code{identical()} is not,
and \code{dta_table_content_hash()} digests the storage. Hence the \code{is.double()}
clause: it is the STORAGE that has to survive, not the class.
}

A frame holding any of them is typed through Arrow instead, so its stamp is
taken from the same frame \code{dta_table_change_signal()} would hash. The cost of
being wrong is not a wrong verdict -- the stamp is still a digest of real
contents, so it can never claim "unchanged" for data that changed -- but a
table rebuilt from its own \code{as.data.frame()} would hash differently from its
stamp and be rescanned on every \code{check()}, which is the one cost the stamp
exists to avoid.

The list is a whitelist, not a blacklist: an R type Arrow handles in some way
nobody here has checked takes the slower, always-correct route.
}
\keyword{internal}
