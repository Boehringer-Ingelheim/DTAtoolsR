% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importConversion.R
\name{dta_table_content_hash}
\alias{dta_table_content_hash}
\title{Digest of a Table's Contents and Carried Import Issues}
\usage{
dta_table_content_hash(df)
}
\arguments{
\item{df}{A data.frame, typically carrying a \code{"dta_import_issues"} attribute.}
}
\value{
A length-1 character digest.
}
\description{
The one content hash both the stamp \code{\link[=dta_stamp_table_hash]{dta_stamp_table_hash()}} records and the
fallback in \code{dta_table_change_signal()} derive, so that a stamped table and
the same table rebuilt from its own \code{as.data.frame()} hash identically.

\code{rlang::hash()} of a data frame alone cannot do that: it digests the object
including its attributes IN ORDER, and the Arrow round trip returns
\code{dta_import_issues} and \code{class} in the opposite order to the frame the stamp
was taken from. Every table carrying import issues therefore hashed one way
at the choke point and another when read back, so a rebuilt table was
rescanned by \code{check()} on every run -- the exact cost the stamp exists to
avoid.

Hashing a canonical list instead removes attribute order from the answer
entirely: the column names, the column vectors stripped of their names, and
the issues frame reduced the same way plus the exact error count it carries
(which is not \code{nrow()} when the per-column cap truncated it, and must change
the signal when it changes).
}
\keyword{internal}
