% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluateRules.R
\name{dta_normalize_conditions}
\alias{dta_normalize_conditions}
\title{Normalise a condition mapping}
\usage{
dta_normalize_conditions(conditions, arg = "condition")
}
\arguments{
\item{conditions}{The raw clause as supplied by the user or the YAML parser.}

\item{arg}{Name of the clause, used in error messages.}
}
\value{
A named list of column conditions, possibly empty.
}
\description{
Brings the \code{condition} / \code{then} clause of a conditional rule into the
canonical named-list form \verb{list(<column> = list(<operator> = <value>))}.

YAML authors legitimately write the clause as a sequence of single-column
mappings:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{condition:
  - VISIT:
      equals: V03
}\if{html}{\out{</div>}}

which \code{yaml::read_yaml()} parses into an \strong{unnamed} list. That form is
unambiguous (a column may be constrained only once), so it is accepted and
folded into the named form rather than rejected. Anything that cannot be
interpreted -- a bare character string, an entry that does not name its
column, or the same column named twice -- aborts with an explicit message.
}
\keyword{internal}
