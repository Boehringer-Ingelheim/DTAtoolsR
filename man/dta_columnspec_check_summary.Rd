% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columnSpecChecks.R
\name{dta_columnspec_check_summary}
\alias{dta_columnspec_check_summary}
\title{Per-Check Summary of the Column Spec Axis}
\usage{
dta_columnspec_check_summary(schemas, tally = NULL, settled = NULL)
}
\arguments{
\item{schemas}{Compiled schemas, from \code{dta_compile_columnspec_schemas()}.}

\item{tally}{A tally, from \code{dta_columnspec_error_tally()}. Defaults to the
zero state.}

\item{settled}{Character. The keywords whose absence of violations may be
reported as a pass. Defaults to all of them; pass a subset (or
\code{character(0)}) for a scan that could not settle them.}
}
\value{
A data frame with one row per keyword and columns \code{check},
\code{keyword}, \code{status}, \code{columns_declared}, \code{columns_checked},
\code{columns_failed}, \code{n_errors} and \code{failed_columns} (the failing column
names, \code{NA} when there are none).
}
\description{
Builds the fixed-shape frame carried as \code{details$columnspec_checks}: one row
per constraint keyword, saying whether that check passed, failed, was never
applicable, or could not be settled.

The four statuses are deliberately distinct. \code{not_applicable} means no column
declares the constraint, so there was nothing to check; \code{not_checked} means
there was, but the scan could not reach a verdict -- a table with no rows, a
\code{fail_fast} run that stopped at the first problem, a structural early return
that read no rows at all, or a constraint whose every declaring column is
absent from the table. Neither is a pass, and neither is a failure. A keyword
with violations is \code{failed} whatever else is true of the scan: a found error
is certain even when the scan that found it was cut short.

A column the table does not have is subtracted from every check but
\code{required}. Its type, length, pattern and permitted values were all
undefined, not satisfied -- and reporting "format check passed (4 columns)"
for a table holding three of them stated a result for a column nothing ever
looked at.
}
\keyword{internal}
